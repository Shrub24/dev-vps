# secrets/.templates/applications/edge-ingress.yaml – edge proxy secrets
# Encrypted to: oci-melb-1 (edge ingress host)
# Key path: secrets/applications/edge-ingress.yaml
cloudflare:
  dns_api_token: <value>