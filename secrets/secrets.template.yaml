# Unencrypted template for reference only.
# Copy values into secrets/secrets.yaml and encrypt with sops.

codenomad:
  env: |
    CODENOMAD_SERVER_USERNAME=saurabhj
    CODENOMAD_SERVER_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

tailscale:
  auth_key: REPLACE_WITH_TAILSCALE_AUTH_KEY

github:
  token: REPLACE_WITH_GITHUB_TOKEN
