---
phase: quick-260328-lf9-switch-termix-tailscale-serve-from-path-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/applications/admin.nix
  - modules/services/termix.nix
  - tests/phase-03-access-contract.sh
  - .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md
autonomous: true
requirements:
  - QUICK-260328-LF9-01
must_haves:
  truths:
    - "Tailnet users reach Termix on a dedicated Tailscale HTTPS port (`8443`) instead of the `/termix` path route."
    - "Termix itself stays local-only HTTP on `127.0.0.1:8083` with no native app TLS."
    - "`modules/services/tailscale.nix` remains application-agnostic while admin-layer wiring owns the Termix Serve contract."
    - "Regression checks and the operator runbook fail fast if stale `/termix` path assumptions return."
  artifacts:
    - path: "modules/applications/admin.nix"
      provides: "Admin-layer Tailscale Serve service for dedicated HTTPS port 8443"
      contains: "tailscale serve --yes --bg --https=8443 http://127.0.0.1:8083"
    - path: "modules/services/termix.nix"
      provides: "Local HTTP-only Termix backend without `/termix` base-path coupling"
      contains: "127.0.0.1:8083:8080"
    - path: "tests/phase-03-access-contract.sh"
      provides: "Executable regression checks for dedicated-port Serve contract"
      contains: "8443"
    - path: ".planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md"
      provides: "Operator verification steps for dedicated-port Tailscale Serve"
      contains: "tailscale serve status"
  key_links:
    - from: "modules/applications/admin.nix"
      to: "modules/services/termix.nix"
      via: "application import"
      pattern: "modules/services/termix\\.nix"
    - from: "modules/applications/admin.nix"
      to: "127.0.0.1:8083"
      via: "dedicated-port tailscale serve reverse proxy target"
      pattern: "127\\.0\\.0\\.1:8083"
    - from: "tests/phase-03-access-contract.sh"
      to: "modules/applications/admin.nix"
      via: "fixed-string assertions for 8443 Serve syntax"
      pattern: "8443"
---

<objective>
Switch Termix from path-based Tailscale Serve to a dedicated Tailscale HTTPS port while keeping the backend local-only and the tailscale service module reusable.

Purpose: implement the locked short-term dedicated-port access contract without introducing native Termix TLS, public ingress, or app-specific routing in `modules/services/tailscale.nix`.
Output: updated admin-layer Serve wiring on `8443`, Termix runtime settings aligned to non-path routing, and tests/runbook text that remove stale `/termix` assumptions.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@modules/applications/admin.nix
@modules/services/termix.nix
@modules/services/tailscale.nix
@tests/phase-03-access-contract.sh
@.planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md
@docs/architecture.md
@docs/decisions.md
@.planning/quick/260328-ivn-serve-termix-over-tailscale-serve-https-/260328-ivn-SUMMARY.md

<decisions>
- D-01: Move Termix from path-based `/termix` Serve to dedicated Tailscale HTTPS port access.
- D-02: Prefer dedicated port `8443` unless a codebase constraint blocks it.
- D-03: Keep `modules/services/tailscale.nix` application-agnostic; do not add app-specific routing there.
- D-04: Keep Termix backend HTTP-only on localhost; do not add native app TLS.
- D-05: Keep the exposure private-only through Tailscale Serve; no Funnel or public ingress.
- D-06: This is a short-term contract; do not design future multi-service REST-like routing now.
- D-07: Keep edits surgical and avoid disturbing unrelated dirty worktree changes.
</decisions>

<interfaces>
From `modules/applications/admin.nix` (current state):
```nix
systemd.services.tailscale-serve-termix.serviceConfig = {
  ExecStart = ''
    ${pkgs.tailscale}/bin/tailscale serve --yes --bg --https=443 --set-path /termix/ http://127.0.0.1:8083
  '';
  ExecStop = ''
    ${pkgs.tailscale}/bin/tailscale serve --https=443 off /termix/
  '';
};
```

From `modules/services/termix.nix` (current state):
```nix
environment = {
  GUACD_HOST = "127.0.0.1";
  GUACD_PORT = "4822";
  VITE_BASE_PATH = "/termix/";
};
ports = [
  "127.0.0.1:8083:8080"
];
```

From `modules/services/tailscale.nix`:
```nix
services.tailscale = {
  enable = true;
  openFirewall = false;
  extraSetFlags = [ "--ssh" ];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire Termix from path-based Serve to dedicated HTTPS port 8443</name>
  <files>modules/applications/admin.nix, modules/services/termix.nix</files>
  <action>Per D-01, D-02, D-03, D-04, D-05, and D-06, update `modules/applications/admin.nix` so the admin-owned `tailscale-serve-termix` unit stops using `--set-path /termix` on port 443 and instead advertises a dedicated HTTPS port contract on `8443` targeting `http://127.0.0.1:8083`. Use the matching dedicated-port `off` command per current Tailscale CLI syntax. Keep Serve ownership entirely in the admin application module per D-03; do not add any Termix routing to `modules/services/tailscale.nix`. In `modules/services/termix.nix`, remove the path-coupling setting `VITE_BASE_PATH = "/termix/";` so the app no longer assumes path-prefixed hosting, while preserving the existing localhost-only `127.0.0.1:8083:8080` publish and HTTP-only backend per D-04. Keep the change surgical per D-07: no public firewall openings, no native TLS/cert wiring, no broad module refactors.</action>
  <verify>
    <automated>rg --fixed-strings 'tailscale serve --yes --bg --https=8443 http://127.0.0.1:8083' modules/applications/admin.nix && rg --fixed-strings 'tailscale serve --https=8443 off' modules/applications/admin.nix && ! rg --fixed-strings '/termix' modules/applications/admin.nix && rg --fixed-strings '"127.0.0.1:8083:8080"' modules/services/termix.nix && ! rg --fixed-strings 'VITE_BASE_PATH' modules/services/termix.nix</automated>
  </verify>
  <done>`modules/applications/admin.nix` owns a dedicated-port `8443` Tailscale Serve mapping to local Termix HTTP, and `modules/services/termix.nix` no longer encodes `/termix` path hosting while staying localhost-only and TLS-free.</done>
</task>

<task type="auto">
  <name>Task 2: Replace stale `/termix` contract checks and operator guidance with dedicated-port expectations</name>
  <files>tests/phase-03-access-contract.sh, .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md</files>
  <action>Per D-01, D-02, D-03, D-04, D-05, and D-07, update `tests/phase-03-access-contract.sh` so it asserts the new dedicated-port contract instead of the old path-based one: require the admin module to contain the `--https=8443` Serve command and matching `off` command, require the backend target `http://127.0.0.1:8083`, require the localhost-only Termix port binding, and fail if stale `/termix` or `VITE_BASE_PATH` assumptions remain in the relevant admin/termix/runbook files. Keep existing checks that protect application-agnostic tailscale wiring and private-only posture. Then update `03-OPERATIONS.md` so the private-network verification step still runs `tailscale serve status` but now tells the operator to expect a dedicated HTTPS listener on port `8443` proxying to local `http://127.0.0.1:8083`, not a `/termix` path route. Preserve unrelated break-glass and access assertions exactly as-is.</action>
  <verify>
    <automated>bash tests/phase-03-access-contract.sh && rg --fixed-strings 'tailscale serve status' .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md && rg --fixed-strings '8443' .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md && ! rg --fixed-strings '/termix' .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md</automated>
  </verify>
  <done>Executable regression checks and the day-2 runbook describe only the dedicated-port `8443` Tailscale Serve contract and reject stale `/termix` path-based drift.</done>
</task>

</tasks>

<verification>
Run `bash tests/phase-03-access-contract.sh` and `nix eval --impure --json --expr 'let flake = builtins.getFlake (toString ./.); cfg = flake.nixosConfigurations.oci-melb-1.config; in { termixPorts = cfg.virtualisation.oci-containers.containers.termix.ports; hasServeUnit = builtins.hasAttr "tailscale-serve-termix" cfg.systemd.services; }' | python3 -c 'import json,sys; data=json.load(sys.stdin); assert data["termixPorts"] == ["127.0.0.1:8083:8080"]; assert data["hasServeUnit"] is True'`. Both must pass, and the first command must fail if `/termix` path assumptions reappear.</verification>

<success_criteria>
- `modules/applications/admin.nix` serves Termix through Tailscale HTTPS on dedicated port `8443`, not on `/termix`.
- `modules/services/termix.nix` keeps Termix local HTTP-only on `127.0.0.1:8083:8080` with no native TLS or path-base config.
- `modules/services/tailscale.nix` remains untouched and application-agnostic.
- Phase-03 access checks and the operations runbook describe and enforce the dedicated-port contract while rejecting stale path-based assumptions.
</success_criteria>

<output>
After completion, create `.planning/quick/260328-lf9-switch-termix-tailscale-serve-from-path-/260328-lf9-SUMMARY.md`
</output>
