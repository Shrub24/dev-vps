---
phase: quick-260410-r8p-codify-syncthing-library-write-acl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/applications/music.nix
  - tests/phase-04-service-flow-contract.sh
autonomous: true
requirements:
  - QUICK-260410-R8P-01
must_haves:
  truths:
    - "Syncthing has explicit write ACL on /srv/media/library for folder marker reproducibility."
    - "Phase-04 service-flow contracts assert the new ACL and reject regressions."
---

<objective>
Codify explicit Syncthing write ACL on `/srv/media/library` and update phase contracts.
</objective>

<tasks>
1. Add explicit ACL rules for `user:syncthing:rwx` and default ACL inheritance on `/srv/media/library` in `modules/applications/music.nix`.
2. Update `tests/phase-04-service-flow-contract.sh` to assert the new ACL and remove the legacy guard that rejected `/srv/media/library` ACL entries.
</tasks>

<verification>
Run `bash tests/phase-04-service-flow-contract.sh` and `bash tests/phase-04-syncthing-contract.sh`.
</verification>
