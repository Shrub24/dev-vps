---
phase: quick-260329-2hf-define-sdb-mount-srv-media-and-move-medi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hosts/oci-melb-1/bootstrap-config.nix
  - modules/providers/oci/default.nix
  - modules/storage/disko-root.nix
  - modules/services/syncthing.nix
  - modules/services/navidrome.nix
  - modules/services/slskd.nix
  - tests/phase-03-bootstrap-contract.sh
  - tests/phase-03-operations-contract.sh
  - tests/phase-04-syncthing-contract.sh
  - tests/phase-04-service-flow-contract.sh
  - .planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md
  - docs/architecture.md
  - docs/decisions.md
autonomous: true
requirements:
  - QUICK-260329-2HF-01
must_haves:
  truths:
    - "oci-melb-1 declares a dedicated `/srv/media` mount backed by `/dev/sdb`."
    - "Syncthing, Navidrome, and slskd's shared-library path all move to `/srv/media` while inbox and service-state paths stay on `/srv/data`."
    - "Phase 03/04 contracts and canonical docs fail if media authority drifts back to `/srv/data/media`."
  artifacts:
    - path: "hosts/oci-melb-1/bootstrap-config.nix"
      provides: "Provider-scoped media disk default for OCI host"
      contains: 'mediaDisk = "/dev/sdb";'
    - path: "modules/storage/disko-root.nix"
      provides: "Dedicated ext4 media filesystem mounted at `/srv/media`"
      contains: 'mountpoint = "/srv/media"'
    - path: "modules/services/syncthing.nix"
      provides: "Syncthing media authority on `/srv/media`"
      contains: 'dataDir = "/srv/media";'
  key_links:
    - from: "hosts/oci-melb-1/bootstrap-config.nix"
      to: "modules/providers/oci/default.nix"
      via: "provider default for secondary media disk"
      pattern: "mediaDisk|bootstrapConfig\.mediaDisk"
    - from: "modules/providers/oci/default.nix"
      to: "modules/storage/disko-root.nix"
      via: "disko media disk device binding"
      pattern: "disko\.devices\.disk\.media\.device"
    - from: "modules/services/syncthing.nix"
      to: "modules/services/navidrome.nix"
      via: "shared `/srv/media` path contract"
      pattern: "/srv/media"
---

<objective>
Define a dedicated `/dev/sdb` media mount at `/srv/media` for `oci-melb-1` and move all Nix-managed media consumers to that mount without disturbing the existing `/srv/data` inbox and service-state layout.

Purpose: separate authoritative media storage from `/srv/data` while preserving the current application-owned ingest boundary and service-state structure.
Output: updated storage/provider config, app path rewiring, and matching contract/doc updates.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@hosts/oci-melb-1/bootstrap-config.nix
@modules/providers/oci/default.nix
@modules/storage/disko-root.nix
@modules/applications/music.nix
@modules/services/syncthing.nix
@modules/services/navidrome.nix
@modules/services/slskd.nix
@tests/phase-03-bootstrap-contract.sh
@tests/phase-03-operations-contract.sh
@tests/phase-04-syncthing-contract.sh
@tests/phase-04-service-flow-contract.sh
@.planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md
@docs/architecture.md
@docs/decisions.md
@.planning/quick/260329-1cm-implement-the-long-term-dual-import-inbo/260329-1cm-SUMMARY.md

<decisions>
- D-01: Preserve the existing app-owned ingest boundary at `/srv/data/inbox` and keep inbox/service-state paths on `/srv/data`.
- D-02: Add a dedicated OCI media disk contract for `/dev/sdb` mounted at `/srv/media`.
- D-03: Move Syncthing, Navidrome, and slskd shared-library references from `/srv/data/media` to `/srv/media`.
- D-04: Update verification and docs in the same change so `/srv/media` becomes the enforced authoritative media path.
- D-05: Keep ROADMAP untouched and do not disturb unrelated dirty worktree files.
</decisions>

<interfaces>
From `hosts/oci-melb-1/bootstrap-config.nix`:
```nix
{
  bootstrapDisk = "/dev/sda";
  rootPartitionSize = "20G";
}
```

From `modules/providers/oci/default.nix`:
```nix
disko.devices.disk.main.device = lib.mkDefault bootstrapConfig.bootstrapDisk;
boot.loader.grub.devices = [ bootstrapConfig.bootstrapDisk ];
```

From `modules/storage/disko-root.nix`:
```nix
disko.devices.disk.main = {
  type = "disk";
  content = {
    type = "gpt";
```  };
};
```

From `modules/services/syncthing.nix`:
```nix
services.syncthing = {
  dataDir = "/srv/data/media";
  settings.folders."media".path = "/srv/data/media";
};
```

From `modules/services/navidrome.nix`:
```nix
services.navidrome.settings.MusicFolder = "/srv/data/media";
```

From `modules/services/slskd.nix`:
```nix
services.slskd.settings = {
  directories = {
    downloads = "/srv/data/inbox/slskd";
    incomplete = "/srv/data/slskd/incomplete";
  };
  shares.directories = [ "/srv/data/media" ];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the dedicated `/srv/media` mount contract for OCI</name>
  <files>hosts/oci-melb-1/bootstrap-config.nix, modules/providers/oci/default.nix, modules/storage/disko-root.nix, tests/phase-03-bootstrap-contract.sh</files>
  <action>Per D-02, extend `hosts/oci-melb-1/bootstrap-config.nix` with a host-scoped `mediaDisk = "/dev/sdb";` setting so the secondary media disk is explicit alongside the existing root bootstrap disk. Update `modules/providers/oci/default.nix` to bind a new `disko.devices.disk.media.device` default from `bootstrapConfig.mediaDisk` while leaving the existing main/root disk and GRUB wiring on `bootstrapDisk` unchanged. Update `modules/storage/disko-root.nix` to declare a second disk entry for the media disk that creates one ext4 filesystem labeled distinctly for media and mounts it at `/srv/media`; keep the existing `/srv/data` filesystem and root layout intact, and do not migrate inbox or service-state directories off `/srv/data`. Update `tests/phase-03-bootstrap-contract.sh` so bootstrap verification enforces the new `mediaDisk` literal, provider binding, and `/srv/media` mount contract in addition to the existing root and `/srv/data` assertions.</action>
  <verify>
    <automated>rg --fixed-strings 'mediaDisk = "/dev/sdb";' hosts/oci-melb-1/bootstrap-config.nix && rg --fixed-strings 'disko.devices.disk.media.device = lib.mkDefault bootstrapConfig.mediaDisk;' modules/providers/oci/default.nix && rg --fixed-strings 'mountpoint = "/srv/media"' modules/storage/disko-root.nix && bash tests/phase-03-bootstrap-contract.sh</automated>
  </verify>
  <done>`oci-melb-1` now declares `/dev/sdb` as an explicit secondary media disk, provider wiring exposes it to disko, and bootstrap contracts fail if the `/srv/media` mount disappears.</done>
</task>

<task type="auto">
  <name>Task 2: Move media-consuming services to `/srv/media` without changing ingest boundaries</name>
  <files>modules/services/syncthing.nix, modules/services/navidrome.nix, modules/services/slskd.nix, tests/phase-03-operations-contract.sh, tests/phase-04-syncthing-contract.sh, tests/phase-04-service-flow-contract.sh</files>
  <action>Per D-01 and D-03, update `modules/services/syncthing.nix` so both `dataDir` and the authoritative media folder path use `/srv/media`, and create `/srv/media` via tmpfiles while leaving `/srv/data/syncthing/config` unchanged. Update `modules/services/navidrome.nix` so `MusicFolder = "/srv/media";` while `DataFolder = "/srv/data/navidrome";` stays where it is. Update `modules/services/slskd.nix` so only the shared-library reference in `shares.directories` moves to `/srv/media`; keep `downloads` and `incomplete` under `/srv/data` exactly as they are now. Then update `tests/phase-03-operations-contract.sh`, `tests/phase-04-syncthing-contract.sh`, and `tests/phase-04-service-flow-contract.sh` so they assert the new `/srv/media` media authority and explicitly continue asserting `/srv/data` ownership for inbox/service-state paths.</action>
  <verify>
    <automated>rg --fixed-strings 'dataDir = "/srv/media";' modules/services/syncthing.nix && rg --fixed-strings 'path = "/srv/media";' modules/services/syncthing.nix && rg --fixed-strings 'MusicFolder = "/srv/media";' modules/services/navidrome.nix && rg --fixed-strings '"/srv/media"' modules/services/slskd.nix && rg --fixed-strings '/srv/data/inbox/slskd' modules/services/slskd.nix && bash tests/phase-03-operations-contract.sh && bash tests/phase-04-syncthing-contract.sh && bash tests/phase-04-service-flow-contract.sh</automated>
  </verify>
  <done>All media readers/writers now converge on `/srv/media` as the authoritative library path, while inbox and service-state paths remain rooted on `/srv/data`.</done>
</task>

<task type="auto">
  <name>Task 3: Align runbooks and architecture records to the new media mount</name>
  <files>.planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md, docs/architecture.md, docs/decisions.md</files>
  <action>Per D-04, update `.planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md` so the direct-media-flow contract reads `Syncthing /srv/media (authoritative) -> Navidrome MusicFolder /srv/media` and the no-duplicate-staging section explains that `/srv/media` is the dedicated media mount while `/srv/data/inbox` and `/srv/data/slskd/incomplete` remain separate ingest/state paths. Update `docs/architecture.md` storage/service-data sections to describe the split explicitly: `/srv/data` remains the persistent service-state mount, `/srv/media` is the dedicated media filesystem, and `music-ingest` still owns only the generic inbox boundary. Append an accepted decision to `docs/decisions.md` recording the `/dev/sdb` -> `/srv/media` contract and the service path migration. Keep the edits surgical; do not touch ROADMAP or unrelated historical docs.</action>
  <verify>
    <automated>rg --fixed-strings 'Syncthing /srv/media (authoritative) -> Navidrome MusicFolder /srv/media' .planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md && rg --fixed-strings '/srv/media' docs/architecture.md docs/decisions.md && ! rg --fixed-strings --quiet 'Syncthing /srv/data/media (authoritative)' .planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md</automated>
  </verify>
  <done>Runbooks and canonical docs now describe `/srv/media` as the authoritative media mount and preserve `/srv/data` for inbox and service state.</done>
</task>

</tasks>

<verification>
Run `bash tests/phase-03-bootstrap-contract.sh`, `bash tests/phase-03-operations-contract.sh`, `bash tests/phase-04-syncthing-contract.sh`, and `bash tests/phase-04-service-flow-contract.sh`. Then run `nix flake check --no-build --no-write-lock-file path:.` to confirm the storage and service path rewiring still evaluates cleanly.
</verification>

<success_criteria>
- `hosts/oci-melb-1/bootstrap-config.nix`, `modules/providers/oci/default.nix`, and `modules/storage/disko-root.nix` together declare a dedicated `/dev/sdb` media disk mounted at `/srv/media` without changing the existing root and `/srv/data` contracts.
- `modules/services/syncthing.nix`, `modules/services/navidrome.nix`, and `modules/services/slskd.nix` use `/srv/media` as the authoritative shared-media path while leaving inbox and service-state directories on `/srv/data`.
- Phase 03 and Phase 04 contract tests fail if `/srv/media` disappears or if media authority drifts back to `/srv/data/media`.
- Canonical docs and the phase-04 service-flow runbook reflect the new storage split and preserve the current `music-ingest` inbox model.
</success_criteria>

<output>
After completion, create `.planning/quick/260329-2hf-define-sdb-mount-srv-media-and-move-medi/260329-2hf-SUMMARY.md`
</output>
