---
phase: quick-260328-17w-implement-post-reboot-oci-console-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hosts/oci-melb-1/users.nix
  - modules/providers/oci/default.nix
  - tests/phase-03-access-contract.sh
  - .planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md
  - .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md
autonomous: true
requirements:
  - QUICK-260328-17W-01
must_haves:
  truths:
    - "Operator can rely on declarative SSH authorized keys for both `dev` and `root` after rebuild or reboot."
    - "OCI serial-console recovery remains intentionally configured for `oci-melb-1` after reboot instead of being left to provider defaults."
    - "Phase 03 verification fails automatically if the direct-key or console-recovery guarantees drift."
  artifacts:
    - path: "hosts/oci-melb-1/users.nix"
      provides: "Canonical Nix-managed SSH authorized key list for recovery-capable users"
      contains: "openssh.authorizedKeys.keys = sshKeys;"
    - path: "modules/providers/oci/default.nix"
      provides: "OCI-specific boot/runtime console hardening kept at the provider boundary"
      contains: "console"
    - path: "tests/phase-03-access-contract.sh"
      provides: "Executable assertions for direct SSH key ownership and OCI console recovery posture"
      contains: "users.nix"
    - path: ".planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md"
      provides: "Runbook that ties serial-console recovery to the declared Nix access model"
      contains: "serial console"
  key_links:
    - from: "hosts/oci-melb-1/default.nix"
      to: "modules/providers/oci/default.nix"
      via: "host import"
      pattern: "providers/oci/default\\.nix"
    - from: "hosts/oci-melb-1/default.nix"
      to: "hosts/oci-melb-1/users.nix"
      via: "host import"
      pattern: "\\./users\\.nix"
    - from: "tests/phase-03-access-contract.sh"
      to: "hosts/oci-melb-1/users.nix"
      via: "fixed-string or scoped regex assertions"
      pattern: "users\\.nix"
---

<objective>
Harden `oci-melb-1` so post-reboot access stays recoverable through OCI console workflows and Nix-managed SSH keys.

Purpose: close the remaining gap where break-glass recovery is documented, but the post-reboot console path and authoritative SSH key source are not yet enforced as declared host contracts.
Output: provider-scoped OCI console hardening, direct Nix-managed SSH authorized key ownership, and contract/runbook updates that keep both guarantees verifiable.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@hosts/oci-melb-1/default.nix
@hosts/oci-melb-1/users.nix
@modules/core/base.nix
@modules/providers/oci/default.nix
@tests/phase-03-access-contract.sh
@.planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md
@.planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md
@.planning/quick/260328-0gu-add-break-glass-access-guarantees-as-abo/260328-0gu-SUMMARY.md

<interfaces>
From `hosts/oci-melb-1/default.nix`:
```nix
imports = [
  ../../modules/providers/oci/default.nix
  ./users.nix
];
networking.hostName = "oci-melb-1";
```

From `hosts/oci-melb-1/users.nix`:
```nix
users.mutableUsers = false;
users.users.dev.openssh.authorizedKeys.keys = sshKeys;
users.users.root.openssh.authorizedKeys.keys = sshKeys;
```

From `modules/core/base.nix`:
```nix
services.openssh.settings = {
  PasswordAuthentication = false;
  KbdInteractiveAuthentication = false;
  PermitRootLogin = "prohibit-password";
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make SSH recovery keys and OCI console posture declarative</name>
  <files>hosts/oci-melb-1/users.nix, modules/providers/oci/default.nix</files>
  <action>Per the quick brief, keep the recovery SSH public key(s) in Nix directly rather than relying on OCI metadata, mutable `authorized_keys`, or manual console edits. Update `hosts/oci-melb-1/users.nix` so the canonical `sshKeys` list contains the intended operator/break-glass key material and remains the shared source for both `dev` and `root`, while preserving `users.mutableUsers = false`. In `modules/providers/oci/default.nix`, add OCI-specific post-reboot console hardening so serial-console access is intentionally available after reboot; keep that wiring provider-scoped instead of moving it into shared modules. Reuse the actual OCI console device expected by this host, and do not weaken the existing SSH stance in `modules/core/base.nix` (no password auth, no interactive auth, no public-access changes).</action>
  <verify>
    <automated>rg --fixed-strings 'openssh.authorizedKeys.keys = sshKeys;' hosts/oci-melb-1/users.nix && rg 'console|serial-getty|getty@|kernelParams' modules/providers/oci/default.nix && nix flake check --no-build --no-write-lock-file path:.</automated>
  </verify>
  <done>`oci-melb-1` has a Nix-owned SSH key source for both recovery-capable users, and the OCI provider module explicitly carries the post-reboot console access settings needed for break-glass recovery.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the hardened access path into docs and executable contracts</name>
  <files>tests/phase-03-access-contract.sh, .planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md, .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md</files>
  <action>Expand the Phase 03 access contract so drift in either guarantee fails verification: (1) direct Nix ownership of recovery SSH keys in `hosts/oci-melb-1/users.nix`, and (2) OCI serial-console hardening in `modules/providers/oci/default.nix`. Update `03-BREAKGLASS.md` to state that post-reboot recovery depends on the declared console path plus the Nix-managed authorized key model, and update `03-OPERATIONS.md` only as needed so routine operations point operators back to that hardened break-glass flow. Use fixed-string or tightly scoped regex assertions like the existing contract tests; keep this host-targeted and avoid adding deploy tooling, cloud-init fallback, or public ingress steps.</action>
  <verify>
    <automated>bash tests/phase-03-access-contract.sh && just verify-phase-03</automated>
  </verify>
  <done>The runbook documents the post-reboot OCI recovery path and declarative key source, and routine Phase 03 verification now fails if either the direct-key or console-hardening contract drifts.</done>
</task>

</tasks>

<verification>
Run `bash tests/phase-03-access-contract.sh` and `just verify-phase-03`. Both must pass with the repository alone, proving the direct-key source, OCI console hardening, and break-glass runbook stay synchronized.
</verification>

<success_criteria>
- `hosts/oci-melb-1/users.nix` is the authoritative source of SSH recovery keys for both `dev` and `root`.
- OCI-specific console access needed after reboot is declared in provider-scoped Nix config instead of assumed operationally.
- Break-glass docs and Phase 03 contract tests enforce the same post-reboot recovery story with no password-auth or public-access regression.
</success_criteria>

<output>
After completion, create `.planning/quick/260328-17w-implement-post-reboot-oci-console-access/260328-17w-SUMMARY.md`
</output>
