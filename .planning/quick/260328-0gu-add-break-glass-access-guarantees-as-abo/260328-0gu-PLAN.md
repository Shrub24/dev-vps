---
phase: quick-260328-0gu-add-break-glass-access-guarantees-as-abo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - justfile
  - tests/phase-03-access-contract.sh
  - .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md
  - .planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md
autonomous: true
requirements:
  - QUICK-260328-01
must_haves:
  truths:
    - "Operator can capture a known-good system generation before redeploying oci-melb-1."
    - "Break-glass recovery instructions stay aligned with the actual operator workflow and required commands."
    - "Phase 03 verification fails automatically if break-glass guarantees drift."
  artifacts:
    - path: "justfile"
      provides: "Named operator command for pre-change break-glass baseline capture"
      contains: "breakglass"
    - path: "tests/phase-03-access-contract.sh"
      provides: "Executable assertions for break-glass and access posture"
      contains: "03-BREAKGLASS.md"
    - path: ".planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md"
      provides: "Recovery runbook keyed to recorded known-good generation"
      contains: "switch-to-configuration switch"
  key_links:
    - from: ".planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md"
      to: "justfile"
      via: "pre-redeploy command reference"
      pattern: "just .*breakglass"
    - from: "tests/phase-03-access-contract.sh"
      to: ".planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md"
      via: "fixed-string runbook contract assertions"
      pattern: "03-BREAKGLASS\.md"
---

<objective>
Strengthen the repository’s break-glass posture so Phase 03 access recovery is not only documented, but tied to a known-good rollback target and enforced by routine verification.

Purpose: close the current gap where break-glass exists as a runbook but is not strongly guaranteed as part of the normal redeploy path.
Output: a pre-change break-glass baseline command, updated runbooks, and executable contract coverage for the recovery flow.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@justfile
@tests/phase-03-access-contract.sh
@.planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md
@.planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md
@.planning/phases/03-oci-host-bring-up-and-private-operations/03-oci-host-bring-up-and-private-operations-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a pre-redeploy break-glass baseline capture step</name>
  <files>justfile, .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md, .planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md</files>
  <action>Add a dedicated operator command in `justfile` for break-glass baseline capture before `just redeploy` (for example, a `breakglass-baseline` recipe). It must SSH to the target host using existing `TARGET_HOST`/`TARGET_USER` conventions, print enough information to identify the host, and capture recent system generations so the operator can record the current known-good rollback target. Update `03-OPERATIONS.md` to require this step immediately before redeploy, and update `03-BREAKGLASS.md` so rollback instructions explicitly tell the operator to use the generation recorded from that pre-change step rather than choosing one ad hoc. Keep the workflow host-targeted and do not introduce deploy-rs or public-access changes.</action>
  <verify>
    <automated>just --list | rg "breakglass-baseline" && rg "just breakglass-baseline" .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md && rg "known-good generation|breakglass-baseline" .planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md</automated>
  </verify>
  <done>A named pre-change baseline command exists, the day-2 runbook requires it before redeploy, and the break-glass runbook uses that recorded generation as the rollback anchor.</done>
</task>

<task type="auto">
  <name>Task 2: Make break-glass guarantees part of the executable access contract</name>
  <files>tests/phase-03-access-contract.sh, .planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md, .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md, justfile</files>
  <action>Expand `tests/phase-03-access-contract.sh` so Phase 03 access verification fails if any required break-glass guarantee drifts. Assert the runbook still includes the OCI serial-console path, generation listing command, rollback command, tailscaled restart/status checks, and operator-side validation command. Also assert the new `breakglass-baseline` recipe is present in `justfile` and referenced from `03-OPERATIONS.md`. Use fixed-string or tightly scoped regex assertions so the contract stays precise and low-maintenance. Normalize doc wording only as needed to keep the script deterministic.</action>
  <verify>
    <automated>bash tests/phase-03-access-contract.sh && just verify-phase-03</automated>
  </verify>
  <done>`bash tests/phase-03-access-contract.sh` covers break-glass guarantees end-to-end, and `just verify-phase-03` enforces them as part of the normal Phase 03 verification flow.</done>
</task>

</tasks>

<verification>
Run `bash tests/phase-03-access-contract.sh` directly, then `just verify-phase-03`, and confirm both succeed without requiring live host reachability beyond existing local contract checks.
</verification>

<success_criteria>
- A routine redeploy workflow now includes an explicit pre-change capture of the current known-good system generation.
- The break-glass runbook references that captured generation instead of relying on operator memory.
- Phase 03 access verification fails automatically if the runbook, baseline command, or recovery command set drifts.
</success_criteria>

<output>
After completion, create `.planning/quick/260328-0gu-add-break-glass-access-guarantees-as-abo/260328-0gu-SUMMARY.md`
</output>
