---
phase: quick-260329-1cm-implement-the-long-term-dual-import-inbo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/applications/music.nix
  - modules/services/slskd.nix
  - modules/services/syncthing.nix
  - tests/phase-04-service-flow-contract.sh
  - .planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md
  - docs/architecture.md
  - docs/decisions.md
autonomous: true
requirements:
  - QUICK-260329-1CM-01
must_haves:
  truths:
    - "Music stack owns a generic ingest boundary at `/srv/data/inbox` without moving app-specific permissions into `modules/core/users.nix`."
    - "slskd downloads are confined to `/srv/data/inbox/slskd/{complete,incomplete}` while `/srv/data/media` remains the authoritative library path."
    - "Syncthing and Navidrome stay anchored on `/srv/data/media` and no longer claim generic inbox ownership."
    - "Phase-04 docs and contract tests fail if the music-ingest boundary or slskd confinement drifts."
  artifacts:
    - path: "modules/applications/music.nix"
      provides: "Application-owned music-ingest group and `/srv/data/inbox` root ownership"
      contains: "music-ingest"
    - path: "modules/services/slskd.nix"
      provides: "slskd-specific inbox subtree under `/srv/data/inbox/slskd`"
      contains: "/srv/data/inbox/slskd/complete"
    - path: "tests/phase-04-service-flow-contract.sh"
      provides: "Executable assertions for music-ingest ownership and slskd confinement"
      contains: "music-ingest"
    - path: ".planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md"
      provides: "Operator-facing dual-import inbox runbook"
      contains: "/srv/data/inbox/slskd"
  key_links:
    - from: "modules/applications/music.nix"
      to: "/srv/data/inbox"
      via: "app-layer tmpfiles rule and music-ingest group"
      pattern: "music-ingest|/srv/data/inbox"
    - from: "modules/applications/music.nix"
      to: "modules/services/slskd.nix"
      via: "slskd extra group membership and shared ingest boundary"
      pattern: "extraGroups = \[ \"music-ingest\" \]"
    - from: "tests/phase-04-service-flow-contract.sh"
      to: "modules/applications/music.nix"
      via: "fixed-string contract assertion"
      pattern: "modules/applications/music\\.nix|music-ingest"
---

<objective>
Implement the long-term dual-import inbox ownership model for the music application without disturbing the canonical media flow.

Purpose: move generic ingest ownership to the music application boundary, confine slskd to its own subdirectory, and keep `/srv/data/media` authoritative for Syncthing/Navidrome.
Output: updated music/slskd/syncthing modules plus matching Phase-04 docs and contract coverage.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@modules/applications/music.nix
@modules/services/slskd.nix
@modules/services/syncthing.nix
@modules/services/navidrome.nix
@modules/core/users.nix
@tests/phase-04-syncthing-contract.sh
@tests/phase-04-service-flow-contract.sh
@.planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md
@docs/architecture.md
@docs/decisions.md
@.planning/quick/260328-fax-make-application-groups-and-add-termix/260328-fax-SUMMARY.md

<decisions>
- D-01: Keep `modules/core/users.nix` unchanged; no app-specific permissions move into the shared core users baseline.
- D-02: Introduce an app-level ingest group named `music-ingest`.
- D-03: Generic ingest root remains `/srv/data/inbox` and must be owned from `modules/applications/music.nix`.
- D-04: `slskd` must be confined to `/srv/data/inbox/slskd/{complete,incomplete}`.
- D-05: `/srv/data/media` remains authoritative for Navidrome and Syncthing.
- D-06: Update docs and contract tests in the same change set.
</decisions>

<interfaces>
From `modules/applications/music.nix`:
```nix
imports = [
  ../../modules/services/syncthing.nix
  ../../modules/services/navidrome.nix
  ../../modules/services/slskd.nix
];

services.slskd = {
  domain = "oci-melb-1";
  environmentFile = "/var/lib/slskd/environment";
};
```

From `modules/services/slskd.nix`:
```nix
services.slskd.settings = {
  directories = {
    downloads = "/srv/data/inbox/complete";
    incomplete = "/srv/data/inbox/incomplete";
  };
  shares.directories = [ "/srv/data/media" ];
};
```

From `modules/services/syncthing.nix`:
```nix
services.syncthing = {
  dataDir = "/srv/data/media";
  settings.folders."media".path = "/srv/data/media";
};

systemd.tmpfiles.rules = [
  "d /srv/data/media 0775 syncthing syncthing - -"
  "d /srv/data/inbox 0775 syncthing syncthing - -"
];
```

From `modules/core/users.nix`:
```nix
users.users.dev.extraGroups = [ "wheel" ];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move generic inbox ownership to the music application layer</name>
  <files>modules/applications/music.nix, modules/services/slskd.nix, modules/services/syncthing.nix</files>
  <action>Per D-02 and D-03, update `modules/applications/music.nix` so the music application owns the generic ingest boundary: declare `users.groups.music-ingest = { };`, extend only the `slskd` service account with `extraGroups = [ "music-ingest" ];`, and create `/srv/data/inbox` from this app module with a setgid tmpfiles rule so later ingest producers can share one app-owned root. Per D-04, move slskd downloads from `/srv/data/inbox/{complete,incomplete}` to `/srv/data/inbox/slskd/{complete,incomplete}` and keep all slskd-created directories scoped to that subtree; remove any generic inbox-root tmpfiles ownership from `modules/services/slskd.nix`. Per D-05, leave `shares.directories = [ "/srv/data/media" ]` untouched and remove the generic `/srv/data/inbox` tmpfiles rule from `modules/services/syncthing.nix` so Syncthing no longer claims inbox ownership. Per D-01, do not modify `modules/core/users.nix`.</action>
  <verify>
    <automated>rg --fixed-strings 'users.groups.music-ingest' modules/applications/music.nix && rg --fixed-strings 'extraGroups = [ "music-ingest" ];' modules/applications/music.nix && rg --fixed-strings '/srv/data/inbox/slskd/complete' modules/services/slskd.nix && rg --fixed-strings '/srv/data/inbox/slskd/incomplete' modules/services/slskd.nix && ! rg --fixed-strings --quiet 'music-ingest' modules/core/users.nix && ! rg --fixed-strings --quiet '/srv/data/inbox' modules/services/syncthing.nix</automated>
  </verify>
  <done>The music app owns `/srv/data/inbox` via `music-ingest`, slskd is restricted to `/srv/data/inbox/slskd/*`, Syncthing no longer creates the generic inbox root, and the shared core users baseline remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Align contracts and runbooks to the dual-import inbox model</name>
  <files>tests/phase-04-service-flow-contract.sh, .planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md, docs/architecture.md, docs/decisions.md</files>
  <action>Per D-06, update `tests/phase-04-service-flow-contract.sh` so it asserts `modules/applications/music.nix` owns `/srv/data/inbox` through `music-ingest`, `modules/services/slskd.nix` uses only `/srv/data/inbox/slskd/{complete,incomplete}`, `modules/services/syncthing.nix` still anchors on `/srv/data/media`, and `modules/services/navidrome.nix` still does not read `/srv/data/inbox`. Add a negative guard that fails if `modules/services/syncthing.nix` recreates generic inbox ownership. Update `.planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md` to describe the new split explicitly: `/srv/data/inbox` is the app-owned ingest root, `slskd` writes only under `/srv/data/inbox/slskd`, and `/srv/data/media` remains authoritative. Update `docs/architecture.md` to record the music application as the owner of the generic ingest boundary, and append an accepted decision to `docs/decisions.md` documenting the `music-ingest` group model and slskd confinement. Do not broaden the change into unrelated service or host rewrites.</action>
  <verify>
    <automated>bash tests/phase-04-syncthing-contract.sh && bash tests/phase-04-service-flow-contract.sh && test "$(nix eval --raw path:.#nixosConfigurations.oci-melb-1.config.services.slskd.settings.directories.downloads)" = "/srv/data/inbox/slskd/complete" && nix flake check --no-build --no-write-lock-file path:.</automated>
  </verify>
  <done>Phase-04 verification and docs now describe and enforce an app-owned generic inbox root, slskd-only service subpaths, and `/srv/data/media` as the unchanged authoritative library path.</done>
</task>

</tasks>

<verification>
Run `bash tests/phase-04-syncthing-contract.sh`, `bash tests/phase-04-service-flow-contract.sh`, `test "$(nix eval --raw path:.#nixosConfigurations.oci-melb-1.config.services.slskd.settings.directories.downloads)" = "/srv/data/inbox/slskd/complete"`, and `nix flake check --no-build --no-write-lock-file path:.`. All must pass together.
</verification>

<success_criteria>
- `modules/core/users.nix` remains unchanged while `modules/applications/music.nix` defines the `music-ingest` group and owns `/srv/data/inbox`.
- `modules/services/slskd.nix` is confined to `/srv/data/inbox/slskd/{complete,incomplete}` and still shares only `/srv/data/media`.
- `modules/services/syncthing.nix` and `modules/services/navidrome.nix` remain authoritative on `/srv/data/media` and do not reclaim generic inbox ownership.
- The Phase-04 service-flow contract and runbook fail if the new ingest-boundary model drifts.
</success_criteria>

<output>
After completion, create `.planning/quick/260329-1cm-implement-the-long-term-dual-import-inbo/260329-1cm-SUMMARY.md`
</output>
