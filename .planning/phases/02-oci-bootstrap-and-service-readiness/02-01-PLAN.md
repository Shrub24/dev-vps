---
phase: 02-oci-bootstrap-and-service-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .sops.yaml
  - hosts/oci-melb-1/default.nix
  - hosts/oci-melb-1/secrets.template.yaml
  - .planning/phases/02-oci-bootstrap-and-service-readiness/02-SECRETS-BOOTSTRAP.md
autonomous: true
requirements:
  - SECR-01
  - SECR-02
  - SECR-03
  - SECR-04
must_haves:
  truths:
    - "Operator can keep shared and host-specific secrets in separate encrypted files"
    - "Adding a new host does not automatically grant access to existing host-scoped secrets"
    - "Operator can follow an explicit two-step bootstrap flow where host secrets are introduced after base install"
    - "Tailscale enrollment material for oci-melb-1 is host-scoped"
  artifacts:
    - path: ".sops.yaml"
      provides: "Path-scoped recipient policy for common and host secret files"
    - path: "hosts/oci-melb-1/default.nix"
      provides: "Host config consuming host-scoped tailscale secret path"
    - path: "hosts/oci-melb-1/secrets.template.yaml"
      provides: "Host-scoped secret template for tailscale enrollment material"
    - path: ".planning/phases/02-oci-bootstrap-and-service-readiness/02-SECRETS-BOOTSTRAP.md"
      provides: "Two-step secret bootstrap contract"
  key_links:
    - from: ".sops.yaml"
      to: "hosts/oci-melb-1/secrets.yaml"
      via: "host-specific path_regex recipient rule"
      pattern: "hosts/oci-melb-1/secrets\\.ya?ml"
    - from: "hosts/oci-melb-1/default.nix"
      to: "hosts/oci-melb-1/secrets.yaml"
      via: "sops secret declaration for tailscale auth key"
      pattern: "sopsFile = ../../hosts/oci-melb-1/secrets.yaml"
---

<objective>
Establish the phase-2 secrets topology and two-step bootstrap contract as executable repository state.

Purpose: implement D-03 and D-16..D-18 boundaries while satisfying all Phase 2 requirements (SECR-01..SECR-04) with host-scoped tailscale enrollment and explicit recipient scoping.
Output: path-scoped `.sops.yaml`, host secret template, host secret wiring, and a concrete bootstrap contract doc.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-oci-bootstrap-and-service-readiness/02-CONTEXT.md
@.planning/phases/02-oci-bootstrap-and-service-readiness/02-RESEARCH.md
@.sops.yaml
@hosts/oci-melb-1/default.nix

<interfaces>
From `.sops.yaml` current rule shape:

```yaml
creation_rules:
  - path_regex: ^secrets/(common|secrets)\.ya?ml$
  - path_regex: ^hosts/[^/]+/secrets\.ya?ml$
```

From `hosts/oci-melb-1/default.nix` current secret wiring:

```nix
sops.defaultSopsFile = ../../secrets/secrets.yaml;
sops.secrets.tailscale_auth_key = { ... };
```

Target contract for this plan:

```nix
sops.defaultSopsFile = ../../secrets/common.yaml;
sops.secrets.tailscale_auth_key.sopsFile = ../../hosts/oci-melb-1/secrets.yaml;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce host-safe SOPS path scoping and host secret template</name>
  <read_first>.sops.yaml, secrets/common.template.yaml, hosts/oci-melb-1/default.nix, .planning/phases/02-oci-bootstrap-and-service-readiness/02-CONTEXT.md</read_first>
  <files>.sops.yaml, hosts/oci-melb-1/secrets.template.yaml</files>
  <behavior>
    - Rule set separates `secrets/common.yaml` from `hosts/oci-melb-1/secrets.yaml` (SECR-01, per D-03).
    - Host secret path has explicit host-scoped recipient group and does not rely on broad shared regex (SECR-02, per D-03).
    - Host template includes `tailscale.auth_key` and no fleet-shared keys (SECR-04, per D-09/D-11).
  </behavior>
  <action>Update `.sops.yaml` rules to the exact path splits: `^secrets/common\.ya?ml$` for fleet-shared data, `^secrets/secrets\.ya?ml$` as transitional legacy compatibility, and `^hosts/oci-melb-1/secrets\.ya?ml$` for host-only data (per D-03 and D-21). Keep owner recipient anchor `&owner_age` unchanged. Add host recipient anchor `&oci_melb_1_age age17ygcdry0l4ktsec4qpjufvvtm2q5rlefqgdsnvmax0m2a0elcefs3pslqf` and include it only in the oci host rule (per SECR-02). Create `hosts/oci-melb-1/secrets.template.yaml` with exactly one required key path `tailscale.auth_key: REPLACE_WITH_TAILSCALE_AUTH_KEY` and a note that values must be encrypted into `hosts/oci-melb-1/secrets.yaml` (per D-10/D-11).</action>
  <acceptance_criteria>`.sops.yaml` contains three explicit `path_regex` rules for common, transitional legacy, and `hosts/oci-melb-1`; host rule references `*oci_melb_1_age`; `hosts/oci-melb-1/secrets.template.yaml` exists and contains `tailscale:` then `auth_key:` placeholder.</acceptance_criteria>
  <verify>
    <automated>rg "\^secrets/common\\.ya\?ml\$|\^secrets/secrets\\.ya\?ml\$|\^hosts/oci-melb-1/secrets\\.ya\?ml\$|oci_melb_1_age" .sops.yaml && rg "tailscale:|auth_key:" hosts/oci-melb-1/secrets.template.yaml</automated>
  </verify>
  <done>Secret file boundaries are path-explicit and host template exists for per-host tailscale enrollment material.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire oci host config to common+host secret split</name>
  <read_first>hosts/oci-melb-1/default.nix, .planning/phases/02-oci-bootstrap-and-service-readiness/02-CONTEXT.md, .planning/phases/02-oci-bootstrap-and-service-readiness/02-RESEARCH.md</read_first>
  <files>hosts/oci-melb-1/default.nix</files>
  <behavior>
    - Fleet-shared default secret source is `secrets/common.yaml` (SECR-01, per D-03).
    - Tailscale auth key is loaded from `hosts/oci-melb-1/secrets.yaml` and remains host-scoped (SECR-04, per D-09/D-11).
    - Base bootstrap can still succeed before host secret material is introduced (SECR-03, per D-03/D-10).
  </behavior>
  <action>Modify `hosts/oci-melb-1/default.nix` to set `sops.defaultSopsFile = ../../secrets/common.yaml` and set `sops.secrets.tailscale_auth_key.sopsFile = ../../hosts/oci-melb-1/secrets.yaml`. Keep secret path `/run/secrets/tailscale.auth_key` and mode `0400`. Add `services.tailscale.authKeyFile` only when host secret file exists by wrapping with `lib.mkIf (builtins.pathExists ../../hosts/oci-melb-1/secrets.yaml)` so the base install remains valid without host secret material (per D-03). Keep private-only tags/hostname flags unchanged (per D-11).</action>
  <acceptance_criteria>`hosts/oci-melb-1/default.nix` contains `sops.defaultSopsFile = ../../secrets/common.yaml`; `tailscale_auth_key` has `sopsFile = ../../hosts/oci-melb-1/secrets.yaml`; `authKeyFile` assignment is guarded with `builtins.pathExists ../../hosts/oci-melb-1/secrets.yaml`.</acceptance_criteria>
  <verify>
    <automated>rg "sops.defaultSopsFile = ../../secrets/common.yaml|sopsFile = ../../hosts/oci-melb-1/secrets.yaml|builtins.pathExists ../../hosts/oci-melb-1/secrets.yaml" hosts/oci-melb-1/default.nix && nix eval --raw path:.#nixosConfigurations.oci-melb-1.config.networking.hostName</automated>
  </verify>
  <done>Host secret consumption uses split files and host-scoped tailscale material while preserving two-step bootstrap safety.</done>
</task>

<task type="auto">
  <name>Task 3: Add explicit two-step secret bootstrap runbook for operators</name>
  <read_first>deploy.sh, justfile, docs/architecture.md, .planning/phases/02-oci-bootstrap-and-service-readiness/02-CONTEXT.md</read_first>
  <files>.planning/phases/02-oci-bootstrap-and-service-readiness/02-SECRETS-BOOTSTRAP.md</files>
  <action>Create `.planning/phases/02-oci-bootstrap-and-service-readiness/02-SECRETS-BOOTSTRAP.md` with exact sections `Step A — Base install without host secrets` and `Step B — Introduce host recipient + host secrets`. Include concrete commands: `./deploy.sh &lt;target-ip&gt;`, `sops --encrypt --in-place hosts/oci-melb-1/secrets.yaml`, and `just redeploy TARGET_HOST=&lt;tailscale-name-or-ip&gt; TARGET_USER=dev`. State that break-glass validation is deferred (D-04), orchestration tooling is deferred (D-19), and backup automation is deferred (D-20). Mention that successful Step A does not require host-specific secret file (D-03).</action>
  <acceptance_criteria>Runbook file exists with both step headings; file contains literal commands for deploy, encrypt, and redeploy; file contains text `D-04`, `D-19`, and `D-20` in a deferred section.</acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/02-oci-bootstrap-and-service-readiness/02-SECRETS-BOOTSTRAP.md && rg "Step A — Base install without host secrets|Step B — Introduce host recipient \+ host secrets|\./deploy.sh|sops --encrypt --in-place hosts/oci-melb-1/secrets.yaml|just redeploy|D-04|D-19|D-20" .planning/phases/02-oci-bootstrap-and-service-readiness/02-SECRETS-BOOTSTRAP.md</automated>
  </verify>
  <done>Operator-facing two-step bootstrap contract is explicit, executable, and aligned with deferred boundaries.</done>
</task>

</tasks>

<verification>
Confirm flake evaluation still resolves `oci-melb-1`, and confirm all secret-path contracts are grep-verifiable.
</verification>

<success_criteria>
- SECR-01..SECR-04 are all represented by executable file changes.
- `.sops.yaml` path scoping is explicit for common and host files.
- Host config and operator runbook enforce two-step bootstrap behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-oci-bootstrap-and-service-readiness/02-oci-bootstrap-and-service-readiness-01-SUMMARY.md`
</output>
