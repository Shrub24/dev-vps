---
phase: 02-oci-bootstrap-and-service-readiness
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - hosts/oci-melb-1/default.nix
  - deploy.sh
  - justfile
  - .planning/phases/02-oci-bootstrap-and-service-readiness/02-SERVICE-READINESS.md
autonomous: true
requirements:
  - SECR-03
  - SECR-04
must_haves:
  truths:
    - "Operator can bootstrap OCI host using the canonical nixos-anywhere remote-build path"
    - "Operator can run one canonical update and verification flow for oci-melb-1"
    - "Host composes storage/provider/service modules with startup sequencing intact"
    - "Service readiness checks distinguish process-level checks from secret-dependent functional checks"
  artifacts:
    - path: "hosts/oci-melb-1/default.nix"
      provides: "Host composition wiring and startup sequencing integration"
    - path: "deploy.sh"
      provides: "Canonical bootstrap CLI preserving remote-build install path"
    - path: "justfile"
      provides: "Canonical operator entrypoints for check/install/redeploy/contract verify"
    - path: ".planning/phases/02-oci-bootstrap-and-service-readiness/02-SERVICE-READINESS.md"
      provides: "Tiered readiness validation contract"
  key_links:
    - from: "hosts/oci-melb-1/default.nix"
      to: "modules/services/syncthing.nix"
      via: "host imports"
      pattern: "modules/services/syncthing.nix"
    - from: "deploy.sh"
      to: "flake.nix"
      via: "--flake path:.#oci-melb-1"
      pattern: "--flake \"path:.#oci-melb-1\""
    - from: "justfile"
      to: "nix eval"
      via: "verify-oci-contract target"
      pattern: "verify-oci-contract"
---

<objective>
Wire new module contracts into the host and lock in one canonical operator path for bootstrap, update, and readiness verification.

Purpose: enforce D-01, D-02, D-10..D-12, and D-16..D-18 while explicitly preserving deferred boundaries D-19 and D-20.
Output: host imports updated, operator command surface normalized, and tiered readiness checks documented.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-oci-bootstrap-and-service-readiness/02-CONTEXT.md
@.planning/phases/02-oci-bootstrap-and-service-readiness/02-RESEARCH.md
@.planning/phases/02-oci-bootstrap-and-service-readiness/02-01-PLAN.md
@.planning/phases/02-oci-bootstrap-and-service-readiness/02-02-PLAN.md
@hosts/oci-melb-1/default.nix
@deploy.sh
@justfile

<interfaces>
From `deploy.sh` bootstrap contract to preserve:

```bash
nix run github:nix-community/nixos-anywhere -- \
  --flake "path:.#oci-melb-1" \
  --build-on-remote \
  --target-host "root@${TARGET_HOST}"
```

From `justfile` current update contract to preserve:

```just
redeploy:
  nix run nixpkgs#nixos-rebuild -- switch --flake path:.#oci-melb-1 --target-host ...
```

Target host import contract to establish:

```nix
imports = [
  ../../modules/services/tailscale.nix
  ../../modules/services/syncthing.nix
  ../../modules/services/navidrome.nix
  ../../modules/services/slskd.nix
  ../../modules/profiles/worker-interface.nix
  ...
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose host with new service/profile modules and startup sequencing</name>
  <read_first>hosts/oci-melb-1/default.nix, modules/services/syncthing.nix, modules/services/navidrome.nix, modules/services/slskd.nix, modules/profiles/worker-interface.nix</read_first>
  <files>hosts/oci-melb-1/default.nix</files>
  <behavior>
    - Host imports provider, storage, and all in-scope service modules from `modules/*` boundaries (D-09, D-12).
    - Startup sequencing network -> sync -> consumers is preserved in host-level/systemd wiring (D-14).
    - Host remains private-only (tailscale-first) with no public ingress additions (D-11).
  </behavior>
  <action>Update `hosts/oci-melb-1/default.nix` imports to include `../../modules/services/syncthing.nix`, `../../modules/services/navidrome.nix`, `../../modules/services/slskd.nix`, and `../../modules/profiles/worker-interface.nix` while keeping existing provider/storage imports. Add host-level ordering guard so `navidrome.service` and `slskd.service` both `after` and `wants` `syncthing.service` and `network-online.target` (if not already encoded in modules). Do not add any reverse proxy/public firewall openings; keep trusted interface limited to `tailscale0` (D-11).</action>
  <acceptance_criteria>`hosts/oci-melb-1/default.nix` imports the four new module files; file contains systemd ordering references from consumers to `syncthing.service`; file still contains `networking.firewall.trustedInterfaces = [ "tailscale0" ]` and has no new public port openings.</acceptance_criteria>
  <verify>
    <automated>rg "modules/services/syncthing.nix|modules/services/navidrome.nix|modules/services/slskd.nix|modules/profiles/worker-interface.nix|syncthing.service|network-online.target|trustedInterfaces = \[ \"tailscale0\" \]" hosts/oci-melb-1/default.nix && nix eval path:.#nixosConfigurations.oci-melb-1.config.services.tailscale.enable</automated>
  </verify>
  <done>Host composition reflects Phase 2 module boundaries and startup sequencing contract.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize canonical operator entry path and add contract verification target</name>
  <read_first>deploy.sh, justfile, .planning/phases/02-oci-bootstrap-and-service-readiness/02-RESEARCH.md, .planning/phases/02-oci-bootstrap-and-service-readiness/02-CONTEXT.md</read_first>
  <files>deploy.sh, justfile, .planning/phases/02-oci-bootstrap-and-service-readiness/02-SERVICE-READINESS.md</files>
  <action>Keep `deploy.sh` as canonical install entry and preserve exact `nixos-anywhere` remote build flags (`--build-on-remote`, `--flake path:.#oci-melb-1`) per D-01/D-02. In `justfile`, keep `redeploy` using `nixos-rebuild --target-host` (D-16) and add `verify-oci-contract` target that runs: `nix flake check --no-build --no-write-lock-file path:.`, `nix eval --raw path:.#nixosConfigurations.oci-melb-1.config.networking.hostName`, `nix eval path:.#nixosConfigurations.oci-melb-1.config.services.tailscale.enable`, `nix eval path:.#nixosConfigurations.oci-melb-1.config.services.syncthing.enable`, `nix eval path:.#nixosConfigurations.oci-melb-1.config.services.navidrome.enable`. Create `.planning/phases/02-oci-bootstrap-and-service-readiness/02-SERVICE-READINESS.md` with mandatory process-level checks (`systemctl is-enabled`/`is-active` for tailscaled, syncthing, navidrome, slskd) and a deferred subsection for secret-dependent functional probes (D-10). Explicitly state `deploy-rs`/Colmena and backup automation remain deferred (D-19, D-20).</action>
  <acceptance_criteria>`deploy.sh` still contains `--build-on-remote` and `--flake "path:.#oci-melb-1"`; `justfile` contains `verify-oci-contract:` target with all listed eval commands; readiness doc exists and contains `systemctl is-enabled tailscaled syncthing navidrome slskd` plus deferred section mentioning D-19 and D-20.</acceptance_criteria>
  <verify>
    <automated>rg "--build-on-remote|--flake \"path:.#oci-melb-1\"" deploy.sh && rg "verify-oci-contract|services.syncthing.enable|services.navidrome.enable|services.tailscale.enable" justfile && test -f .planning/phases/02-oci-bootstrap-and-service-readiness/02-SERVICE-READINESS.md && rg "systemctl is-enabled|tailscaled|syncthing|navidrome|slskd|D-19|D-20" .planning/phases/02-oci-bootstrap-and-service-readiness/02-SERVICE-READINESS.md</automated>
  </verify>
  <done>One canonical operator command surface exists for bootstrap/redeploy/contract checks, with tiered service readiness guidance.</done>
</task>

</tasks>

<verification>
Run `just verify-oci-contract` and ensure service enable flags evaluate plus command surfaces remain consistent.
</verification>

<success_criteria>
- Host composition consumes new module contracts with no provider-boundary violations.
- Canonical operator path is stable (`deploy.sh` install, `just redeploy` updates, `just verify-oci-contract` checks).
- Tiered readiness policy is documented and aligned with deferred deep checks.
</success_criteria>

<output>
After completion, create `.planning/phases/02-oci-bootstrap-and-service-readiness/02-oci-bootstrap-and-service-readiness-03-SUMMARY.md`
</output>
