---
phase: 02-oci-bootstrap-and-service-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/storage/disko-root.nix
  - modules/services/syncthing.nix
  - modules/services/navidrome.nix
  - modules/services/slskd.nix
  - modules/profiles/worker-interface.nix
autonomous: true
requirements:
  - SECR-03
  - SECR-04
must_haves:
  truths:
    - "Host storage layout includes one canonical persistent data mount with stable identifier usage"
    - "Service modules for syncthing, navidrome, and slskd exist with private-first defaults"
    - "Startup ordering is encoded as network -> sync -> consumers"
    - "Worker boundary is defined as an interface without implementing worker services"
  artifacts:
    - path: "modules/storage/disko-root.nix"
      provides: "GPT + EFI + ext4 root + ext4 data filesystem contract"
    - path: "modules/services/syncthing.nix"
      provides: "Sync service module and canonical media path ownership"
    - path: "modules/services/navidrome.nix"
      provides: "Music consumer service module reading canonical media path"
    - path: "modules/services/slskd.nix"
      provides: "Ingest service module writing to staging inbox"
    - path: "modules/profiles/worker-interface.nix"
      provides: "Deferred worker interface contract"
  key_links:
    - from: "modules/services/slskd.nix"
      to: "/srv/data/inbox"
      via: "download directory configuration"
      pattern: "/srv/data/inbox"
    - from: "modules/services/navidrome.nix"
      to: "/srv/data/media"
      via: "music folder configuration"
      pattern: "/srv/data/media"
    - from: "modules/services/navidrome.nix"
      to: "syncthing.service"
      via: "systemd service ordering"
      pattern: "after = \[ .*syncthing"
---

<objective>
Define Phase 2 storage and service-readiness contracts as reusable modules, aligned with the user’s data and startup sequencing decisions.

Purpose: implement D-05..D-15 without introducing deferred worker implementations, backup automation, or orchestration tooling.
Output: storage expansion, three service modules, and a worker-interface profile boundary.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-oci-bootstrap-and-service-readiness/02-CONTEXT.md
@.planning/phases/02-oci-bootstrap-and-service-readiness/02-RESEARCH.md
@modules/storage/disko-root.nix
@modules/services/tailscale.nix

<interfaces>
From `modules/storage/disko-root.nix` current contract:

```nix
disko.devices.disk.main.content.partitions = {
  ESP = { ... mountpoint = "/boot"; };
  root = { ... mountpoint = "/"; };
};
```

Target storage contract for this plan:

```nix
root = { ... mountpoint = "/"; };
data = { ... mountpoint = "/srv/data"; };
```

Service module contract pattern (from existing tailscale module):

```nix
{ ... }:
{ services.<name>.enable = true; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend disko layout to canonical root+data baseline</name>
  <read_first>modules/storage/disko-root.nix, modules/providers/oci/default.nix, .planning/phases/02-oci-bootstrap-and-service-readiness/02-CONTEXT.md</read_first>
  <files>modules/storage/disko-root.nix</files>
  <behavior>
    - Disk layout is GPT + EFI + ext4 root + ext4 data as required (D-05).
    - Service data mount is singular and canonical at `/srv/data` with predictable ownership target paths (D-06, D-08).
    - Mount declarations avoid transient `/dev/sdX` assumptions by using labels/UUID-derived filesystem wiring (D-07).
  </behavior>
  <action>Modify `modules/storage/disko-root.nix` so partition map contains `ESP`, `root`, and `data`. Keep `ESP` at `512M` with vfat mount `/boot`. Change `root` size to `60G`, ext4, label `rootfs`, mount `/`. Add `data` partition using remaining disk (`size = "100%"`), ext4 label `srv-data`, mount `/srv/data`. Add mount options for `/srv/data`: `nofail` and `x-systemd.device-timeout=10s`. Do not create additional mounts beyond `/` and `/srv/data` (per D-06).</action>
  <acceptance_criteria>`modules/storage/disko-root.nix` contains partition names `root` and `data`; `/srv/data` appears as mountpoint; ext4 labels `rootfs` and `srv-data` appear; there is exactly one non-root persistent service mount (`/srv/data`).</acceptance_criteria>
  <verify>
    <automated>rg "rootfs|srv-data|mountpoint = \"/srv/data\"|x-systemd.device-timeout=10s|nofail" modules/storage/disko-root.nix && nix eval --raw path:.#nixosConfigurations.oci-melb-1.config.fileSystems."/srv/data".mountPoint</automated>
  </verify>
  <done>Storage module encodes the required root+data baseline and canonical service data mount.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add private-first service modules and worker interface boundary</name>
  <read_first>modules/services/tailscale.nix, hosts/oci-melb-1/default.nix, .planning/phases/02-oci-bootstrap-and-service-readiness/02-CONTEXT.md</read_first>
  <files>modules/services/syncthing.nix, modules/services/navidrome.nix, modules/services/slskd.nix, modules/profiles/worker-interface.nix</files>
  <behavior>
    - `syncthing`, `navidrome`, and `slskd` modules exist and enable their services for readiness (D-09, D-10).
    - Canonical data ownership is represented as `/srv/data/media` plus `/srv/data/inbox` staging flow (D-13).
    - Startup ordering is encoded as network -> sync -> consumers (D-14).
    - Worker profile interface exists, but worker implementation remains deferred (D-15).
  </behavior>
  <action>Create `modules/services/syncthing.nix` enabling `services.syncthing`, defining data/config paths under `/srv/data/syncthing`, and adding tmpfiles rules to create `/srv/data/media` and `/srv/data/inbox`. Create `modules/services/navidrome.nix` enabling `services.navidrome` with `settings.MusicFolder = "/srv/data/media"`, data folder `/srv/data/navidrome`, and systemd ordering `after/wants = [ "network-online.target" "syncthing.service" ]`. Create `modules/services/slskd.nix` enabling `services.slskd` (or explicit `systemd.services.slskd` if module options unavailable) with download/incomplete path rooted at `/srv/data/inbox`, plus `after/wants = [ "network-online.target" "syncthing.service" ]`. Create `modules/profiles/worker-interface.nix` defining option `fleet.worker.enable = lib.mkEnableOption "future worker interface"` and no worker service units (per D-15 defer implementation).</action>
  <acceptance_criteria>All four files exist; syncthing module contains `/srv/data/media` and `/srv/data/inbox`; navidrome module contains `MusicFolder = "/srv/data/media"`; slskd module contains `/srv/data/inbox`; worker interface defines `fleet.worker.enable` and contains no `systemd.services` definitions.</acceptance_criteria>
  <verify>
    <automated>test -f modules/services/syncthing.nix && test -f modules/services/navidrome.nix && test -f modules/services/slskd.nix && test -f modules/profiles/worker-interface.nix && rg "/srv/data/media|/srv/data/inbox" modules/services/syncthing.nix modules/services/slskd.nix modules/services/navidrome.nix && rg "fleet.worker.enable" modules/profiles/worker-interface.nix && ! rg -q "systemd.services" modules/profiles/worker-interface.nix</automated>
  </verify>
  <done>Service and worker-boundary modules are present with private-first, sequencing-aware contracts and no deferred-worker overreach.</done>
</task>

</tasks>

<verification>
Evaluate `oci-melb-1` config and confirm `/srv/data` and service modules are represented in declarative state.
</verification>

<success_criteria>
- Storage and service contracts encode D-05..D-15 boundaries.
- Data ownership model uses canonical media path plus staging inbox.
- Worker boundary is declared without implementing worker services.
</success_criteria>

<output>
After completion, create `.planning/phases/02-oci-bootstrap-and-service-readiness/02-oci-bootstrap-and-service-readiness-02-SUMMARY.md`
</output>
