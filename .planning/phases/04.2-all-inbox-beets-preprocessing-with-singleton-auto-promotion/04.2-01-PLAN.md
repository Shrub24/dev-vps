---
phase: 04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-04.2-beets-promotion-contract.sh
  - modules/services/beets-inbox.nix
  - tests/phase-04-service-flow-contract.sh
autonomous: true
requirements:
  - MEDI-01
user_setup:
  - service: beatport
    why: "Re-authorize the built-in Beatport plugin after the 04.2 worker changes so full metadata-source behavior remains available"
    dashboard_config:
      - task: "Complete the first interactive Beatport browser/paste-back auth flow from the target host after deploy so tokens persist under /srv/data/beets"
        location: "Beatport OAuth page opened by the Beets runtime"
must_haves:
  truths:
    - "Operator can drop audio anywhere under /srv/media/inbox and successful files are auto-promoted into /srv/media/library without renaming the filename."
    - "A hard failure leaves only that file in inbox while other files in the same run continue to promote."
    - "Navidrome still sees the whole /srv/media tree, so failed inbox items remain playable while promoted items appear under /srv/media/library."
  artifacts:
    - path: "tests/phase-04.2-beets-promotion-contract.sh"
      provides: "Executable promotion/regression contract for all-inbox Beets behavior"
    - path: "modules/services/beets-inbox.nix"
      provides: "All-inbox Beets worker with per-file promotion, timer backstop, and /srv/data/beets reporting"
      exports: ["systemd.paths.beets-inbox-watch", "systemd.timers.beets-inbox-backstop", "systemd.services.beets-inbox-run"]
    - path: "tests/phase-04-service-flow-contract.sh"
      provides: "Baseline media-flow guard that keeps Navidrome rooted on /srv/media"
  key_links:
    - from: "modules/services/beets-inbox.nix"
      to: "/srv/media/inbox"
      via: "runner default root and path-trigger scope"
      pattern: 'TARGET_PATH="${1:-/srv/media/inbox}"'
    - from: "modules/services/beets-inbox.nix"
      to: "/srv/media/library"
      via: "promotion destination directory"
      pattern: 'LIBRARY_ROOT="/srv/media/library"'
    - from: "modules/services/beets-inbox.nix"
      to: "/srv/data/beets"
      via: "BEETSDIR + report/unresolved roots"
      pattern: 'BEETSDIR=/srv/data/beets'
---

<objective>
Expand the existing Beets worker into an all-inbox, per-file auto-promotion pipeline.

Purpose: implement MEDI-01 by introducing the first inbox-to-library promotion flow without breaking `/srv/media` authority, filename preservation, or playback visibility.
Output: promotion contract test, evolved `modules/services/beets-inbox.nix`, and baseline service-flow guards updated for the new library subtree.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-CONTEXT.md
@.planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-RESEARCH.md
@.planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-VALIDATION.md
@.planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-BEETS-WORKER.md
@.planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md
@modules/services/beets-inbox.nix
@modules/services/navidrome.nix
@tests/phase-04.1-beets-contract.sh
@tests/phase-04-service-flow-contract.sh

<interfaces>
Current Beets worker entrypoint and watch scope:

```nix
ExecStart = "${beetsInboxRunner}/bin/beets-inbox-runner /srv/media/inbox";

systemd.paths.beets-inbox-watch = {
  wantedBy = [ "multi-user.target" ];
  pathConfig.PathExistsGlob = "/srv/media/inbox/*";
  pathConfig.Unit = "beets-inbox-run.service";
};
```

Current generated Beets config surface:

```nix
directory: /srv/media/inbox
library: /srv/data/beets/state/library.db
plugins: discogs beatport bandcamp fromfilename
import:
  write: yes
  copy: no
  move: no
  link: no
  hardlink: no
  quiet: yes
  quiet_fallback: skip
```

Current playback visibility contract:

```nix
services.navidrome.settings = {
  MusicFolder = "/srv/media";
  DataFolder = "/srv/data/navidrome";
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the 04.2 promotion contract before changing the worker</name>
  <read_first>tests/phase-04.1-beets-contract.sh, tests/phase-04-service-flow-contract.sh, modules/services/beets-inbox.nix, modules/services/navidrome.nix, .planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-CONTEXT.md</read_first>
  <files>tests/phase-04.2-beets-promotion-contract.sh, tests/phase-04-service-flow-contract.sh</files>
  <behavior>
    - Test fails if the worker remains slskd-only instead of scanning `/srv/media/inbox` broadly.
    - Test fails if promotion to `/srv/media/library` is missing, if filenames are renamed, or if hard-failure reporting under `/srv/data/beets` is missing.
    - Test fails if Navidrome is narrowed to `/srv/media/library` or if any copy/move/link/hardlink import mode is reintroduced inside Beets itself.
  </behavior>
  <action>Create executable `tests/phase-04.2-beets-promotion-contract.sh` with `#!/usr/bin/env bash`, `set -euo pipefail`, and fixed-string assertions against `modules/services/beets-inbox.nix` plus `tests/phase-04-service-flow-contract.sh`. Encode these exact expected strings for the evolved worker: `TARGET_PATH="${1:-/srv/media/inbox}"`, `LIBRARY_ROOT="/srv/media/library"`, `quiet_fallback: asis`, `plugins: discogs beatport bandcamp fromfilename`, `/srv/data/beets/reports`, `/srv/data/beets/unresolved`, `systemd.timers.beets-inbox-backstop`, `OnUnitActiveSec = "15m"`, `basename "$file"`, and `beet -c /srv/data/beets/config.yaml import -q -s -C`. Add explicit negative guards that fail if `PathExistsGlob = "/srv/media/inbox/slskd/*"`, `MusicFolder = "/srv/media/library"`, `move: yes`, `copy: yes`, `link: yes`, `hardlink: yes`, `soundcloud`, or any source-preserving final path like `/srv/media/library/slskd` appears. Update `tests/phase-04-service-flow-contract.sh` so it still asserts `MusicFolder = "/srv/media"`, explicitly rejects `MusicFolder = "/srv/media/library"`, and allows `/srv/media/library` to exist elsewhere as a promoted subtree.</action>
  <acceptance_criteria>`tests/phase-04.2-beets-promotion-contract.sh` exists, is executable, contains fixed-string checks for `TARGET_PATH="${1:-/srv/media/inbox}"`, `LIBRARY_ROOT="/srv/media/library"`, `quiet_fallback: asis`, `systemd.timers.beets-inbox-backstop`, `OnUnitActiveSec = "15m"`, `basename "$file"`, and `/srv/data/beets/unresolved`; `tests/phase-04-service-flow-contract.sh` contains both `MusicFolder = "/srv/media"` and a rejection branch for `MusicFolder = "/srv/media/library"`.</acceptance_criteria>
  <verify>
    <automated>test -x tests/phase-04.2-beets-promotion-contract.sh && rg --fixed-strings 'quiet_fallback: asis' tests/phase-04.2-beets-promotion-contract.sh && rg --fixed-strings 'MusicFolder = "/srv/media/library"' tests/phase-04-service-flow-contract.sh</automated>
  </verify>
  <done>The phase has a failing-first executable specification for all-inbox promotion, preserved filenames, and broad playback visibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evolve the Beets worker into per-file all-inbox promotion</name>
  <read_first>modules/services/beets-inbox.nix, tests/phase-04.2-beets-promotion-contract.sh, tests/phase-04-service-flow-contract.sh, modules/services/navidrome.nix, .planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-RESEARCH.md</read_first>
  <files>modules/services/beets-inbox.nix, tests/phase-04-service-flow-contract.sh</files>
  <behavior>
    - Successful files promote into `/srv/media/library/<top-level>/<release>/<original filename>` even when metadata is imperfect.
    - Only hard failures remain in inbox, and sibling files continue processing in the same run.
    - Navidrome keeps reading all of `/srv/media`, so inbox failures remain playable while promoted files appear under the library subtree.
  </behavior>
  <action>Update `modules/services/beets-inbox.nix` to implement D-01 through D-15 with these concrete values: keep the existing `python3.withPackages` runtime but change the generated config to `directory: /srv/media/library`, `library: /srv/data/beets/state/library.db`, `plugins: discogs beatport bandcamp fromfilename`, and `import:` values `write: yes`, `copy: no`, `move: no`, `link: no`, `hardlink: no`, `quiet: yes`, `quiet_fallback: asis`, `incremental: no`, `incremental_skip_later: no`. In the runner, set `ROOT_INBOX="/srv/media/inbox"` and `LIBRARY_ROOT="/srv/media/library"`, scan recursively for supported audio files older than 120 seconds, and process them one file at a time with `for file in "${CANDIDATES[@]}"; do ... done` so errors on one file do not abort the rest. For each file: run `beet -c /srv/data/beets/config.yaml import -q -s -C "$file"`; if the command succeeds, compute destination directories with a small embedded Python helper using this rule per D-05/D-06/D-07: choose top-level directory from `label` when `label` exists and `albumartist` is empty or `Various Artists`, otherwise use `albumartist`, then `artist`, then `Unknown Artist`; choose release directory from `album`, then `title`, then the source stem, then move the file to `/srv/media/library/<top-level>/<release>/<original basename>` using `basename "$file"` so filenames never change (D-08). If the import exits non-zero, metadata extraction fails, destination directory creation fails, or `mv` fails, leave the file in its existing inbox path and append one tab-separated row to `/srv/data/beets/unresolved/<timestamp>-hard-failures.tsv` containing source path and error reason (D-13/D-14/D-15). Also append imperfect-but-promoted rows to `/srv/data/beets/reports/<timestamp>-promoted-with-fallback.tsv` whenever the promotion used `Unknown Artist`, title fallback, or label/artist fallback. Keep `/srv/data/beets/reports/<timestamp>-summary.txt` with counts for promoted, promoted_with_fallback, and hard_failures. Add `systemd.tmpfiles.rules` for `d /srv/media/library 2775 syncthing music-library - -`. Keep `systemd.paths.beets-inbox-watch` for the fast path, but add `systemd.timers.beets-inbox-backstop` with `OnBootSec = "5m"` and `OnUnitActiveSec = "15m"` to rerun the same oneshot service against `/srv/media/inbox`. Update `tests/phase-04-service-flow-contract.sh` only as needed to preserve the phase-04 rule that Navidrome continues to read `MusicFolder = "/srv/media"` rather than `/srv/media/library` only. Do not add a review bucket, do not park failures, do not narrow Navidrome, do not preserve source-folder identity in the promoted path, and do not rename filenames.</action>
  <acceptance_criteria>`modules/services/beets-inbox.nix` contains `quiet_fallback: asis`, `LIBRARY_ROOT="/srv/media/library"`, `basename "$file"`, `/srv/data/beets/reports`, `/srv/data/beets/unresolved`, `systemd.timers.beets-inbox-backstop`, `OnBootSec = "5m"`, `OnUnitActiveSec = "15m"`, and `d /srv/media/library 2775 syncthing music-library - -`; `tests/phase-04-service-flow-contract.sh` still enforces `MusicFolder = "/srv/media"` and rejects `MusicFolder = "/srv/media/library"`.</acceptance_criteria>
  <verify>
    <automated>bash tests/phase-04.2-beets-promotion-contract.sh && bash tests/phase-04-service-flow-contract.sh</automated>
  </verify>
  <done>The repository has a declarative all-inbox Beets worker that auto-promotes usable files into `/srv/media/library`, preserves filenames, leaves only hard failures in inbox, and preserves broad playback visibility.</done>
</task>

</tasks>

<verification>
Run `bash tests/phase-04.2-beets-promotion-contract.sh` and `bash tests/phase-04-service-flow-contract.sh`; both must exit 0 before the docs/ops layer starts.
</verification>

<success_criteria>
- MEDI-01 is advanced by a real inbox-to-library promotion flow rooted on `/srv/media`.
- Locked decisions D-01 through D-15 are encoded directly in the worker behavior and regression checks.
- No filename renaming, review bucket, or library-only playback narrowing is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion-01-SUMMARY.md`
</output>
