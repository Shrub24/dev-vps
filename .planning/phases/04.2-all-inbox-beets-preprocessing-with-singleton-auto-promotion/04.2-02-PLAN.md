---
phase: 04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion
plan: 02
type: execute
wave: 2
depends_on:
  - "04.2-01"
files_modified:
  - justfile
  - .planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-BEETS-PROMOTION.md
  - docs/decisions.md
  - docs/architecture.md
  - docs/plan.md
autonomous: true
requirements:
  - MEDI-01
must_haves:
  truths:
    - "Operator can verify phase 04.2 with one command that reruns the phase-04 baseline plus the 04.2 promotion contract."
    - "Operator can follow one runbook for automatic promotion, dry-run testing, fallback reports, and hard-failure handling."
    - "Canonical docs explain that 04.2 supersedes the 04.1 no-promotion boundary with all-inbox auto-promotion while keeping `/srv/media` playback visibility."
  artifacts:
    - path: "justfile"
      provides: "verify-phase-04.2 operator recipe"
    - path: ".planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-BEETS-PROMOTION.md"
      provides: "Phase runbook for all-inbox promotion, reports, and manual targeting"
    - path: "docs/decisions.md"
      provides: "Canonical decision update from 04.1 no-promotion to 04.2 promotion behavior"
    - path: "docs/architecture.md"
      provides: "Canonical storage/media-flow description including /srv/media/library subtree"
    - path: "docs/plan.md"
      provides: "Strategic plan updated to reflect native systemd promotion evolution without app-based review"
  key_links:
    - from: "justfile"
      to: "tests/phase-04.2-beets-promotion-contract.sh"
      via: "verify-phase-04.2 recipe"
      pattern: "bash tests/phase-04.2-beets-promotion-contract.sh"
    - from: ".planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-BEETS-PROMOTION.md"
      to: "beets-inbox-runner"
      via: "documented command surface"
      pattern: "beets-inbox-runner /srv/media/inbox"
    - from: "docs/decisions.md"
      to: "docs/architecture.md"
      via: "04.2 promotion policy mirrored in canonical architecture text"
      pattern: "/srv/media/library"
---

<objective>
Add the operator verification and documentation layer for the 04.2 promotion flow.

Purpose: make MEDI-01 operationally verifiable and keep the 04.2 promotion boundary explicit across command surface and canonical docs.
Output: `verify-phase-04.2` recipe, promotion runbook, and canonical docs updated to reflect the new all-inbox auto-promotion posture.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-CONTEXT.md
@.planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-RESEARCH.md
@.planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-VALIDATION.md
@.planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-BEETS-WORKER.md
@.planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md
@justfile
@docs/decisions.md
@docs/architecture.md
@docs/plan.md

<interfaces>
Existing phase verification aggregation pattern:

```just
verify-phase-04:
  bash tests/phase-04-syncthing-contract.sh
  bash tests/phase-04-service-flow-contract.sh
  just verify-oci-contract
```

Current 04.1 recipe naming workaround:

```just
# verify-phase-04.1:
verify-phase-04-1:
  just verify-phase-04
  bash tests/phase-04.1-beets-contract.sh
```

Planned 04.2 command surface from plan 01:

```text
beets-inbox-runner /srv/media/inbox
BEETS_DRY_RUN=1 beets-inbox-runner /srv/media/inbox
beets-inbox-runner /srv/media/inbox/<selected-subpath>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a single phase verification recipe for the promotion flow</name>
  <read_first>justfile, tests/phase-04.2-beets-promotion-contract.sh, tests/phase-04-service-flow-contract.sh</read_first>
  <files>justfile</files>
  <action>Add the 04.2 aggregate verification recipe to `justfile` using the same dotted-name workaround pattern as 04.1: keep a comment line `# verify-phase-04.2:` and add the runnable recipe `verify-phase-04-2:` with this exact command order: first `just verify-phase-04`, then `bash tests/phase-04.2-beets-promotion-contract.sh`. Do not change `verify-phase-04`; 04.2 verification must layer on top of the baseline rather than replace it.</action>
  <acceptance_criteria>`justfile` contains a comment `# verify-phase-04.2:` and a runnable `verify-phase-04-2:` recipe with the exact lines `just verify-phase-04` and `bash tests/phase-04.2-beets-promotion-contract.sh` in that order.</acceptance_criteria>
  <verify>
    <automated>just verify-phase-04-2</automated>
  </verify>
  <done>Operators can verify the baseline media flow plus 04.2 promotion behavior with one command.</done>
</task>

<task type="auto">
  <name>Task 2: Document the promotion workflow and update canonical docs</name>
  <read_first>.planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-BEETS-PROMOTION.md, .planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-CONTEXT.md, docs/decisions.md, docs/architecture.md, docs/plan.md, .planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-BEETS-WORKER.md</read_first>
  <files>.planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-BEETS-PROMOTION.md, docs/decisions.md, docs/architecture.md, docs/plan.md</files>
  <action>Create `.planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-BEETS-PROMOTION.md` with these exact sections and strings: `Automatic flow` showing `/srv/media/inbox/** -> beets-inbox-run.service -> /srv/media/library/<top-level>/<release>/<original filename> -> /srv/data/beets/reports + /srv/data/beets/unresolved`; `Manual targeting` listing `beets-inbox-runner /srv/media/inbox`, `BEETS_DRY_RUN=1 beets-inbox-runner /srv/media/inbox`, and `beets-inbox-runner /srv/media/inbox/<selected-subpath>`; `Verification` listing `bash tests/phase-04.2-beets-promotion-contract.sh` and `just verify-phase-04.2` plus the note that this repo executes `just verify-phase-04-2`; `Reports` listing `/srv/data/beets/reports/<timestamp>-summary.txt`, `/srv/data/beets/reports/<timestamp>-promoted-with-fallback.tsv`, and `/srv/data/beets/unresolved/<timestamp>-hard-failures.tsv`; and `Guardrails` stating `filenames stay unchanged`, `failed files remain in inbox`, `Navidrome still reads /srv/media`, and `no dedicated review UI or parked retry flow`. Update `docs/decisions.md` by changing D-021 status to `Superseded by D-022` and appending a new D-022 entry that records all-inbox singleton auto-promotion, `/srv/media/library` destination, filename preservation, `/srv/data/beets` reports, and preserved `/srv/media` playback visibility. Update `docs/architecture.md` so the `Storage and Service Data Model` section says `/srv/media/inbox` is the ingest boundary, `/srv/media/library` is the promoted canonical subtree, Navidrome still reads `/srv/media`, and Beets reports remain under `/srv/data/beets`. Update `docs/plan.md` Track D/Track E language so the repository strategy now explicitly includes native systemd-based inbox-to-library promotion evolution while still deferring app-based review UX and higher-complexity orchestration.</action>
  <acceptance_criteria>The new runbook contains the exact strings `beets-inbox-runner /srv/media/inbox`, `BEETS_DRY_RUN=1 beets-inbox-runner /srv/media/inbox`, `/srv/data/beets/reports/<timestamp>-promoted-with-fallback.tsv`, `/srv/data/beets/unresolved/<timestamp>-hard-failures.tsv`, `filenames stay unchanged`, and `Navidrome still reads /srv/media`; `docs/decisions.md` contains both `Superseded by D-022` and a new `D-022` entry mentioning `/srv/media/library`, `/srv/data/beets`, `filename preservation`, and `/srv/media`; `docs/architecture.md` and `docs/plan.md` both mention `/srv/media/library`.</acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-BEETS-PROMOTION.md && rg 'beets-inbox-runner /srv/media/inbox|BEETS_DRY_RUN=1 beets-inbox-runner /srv/media/inbox|/srv/data/beets/reports/<timestamp>-promoted-with-fallback.tsv|/srv/data/beets/unresolved/<timestamp>-hard-failures.tsv|filenames stay unchanged|Navidrome still reads /srv/media' .planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-BEETS-PROMOTION.md && rg 'Superseded by D-022|/srv/media/library|filename preservation|/srv/data/beets|/srv/media' docs/decisions.md && rg '/srv/media/library' docs/architecture.md docs/plan.md</automated>
  </verify>
  <done>The promotion flow has one operator runbook and coherent canonical-doc coverage that no longer leaves 04.1's no-promotion decision as the active policy.</done>
</task>

</tasks>

<verification>
Run `just verify-phase-04-2` and confirm the runbook plus canonical docs all describe the same `/srv/media` + `/srv/media/library` + `/srv/data/beets` contract.
</verification>

<success_criteria>
- MEDI-01 is operationally enforceable through a single verification command and one promotion runbook.
- Canonical docs clearly show that 04.2 replaces the 04.1 no-promotion boundary with all-inbox auto-promotion.
- The operator command surface and canonical docs both preserve the just dotted-name workaround without hiding the canonical `verify-phase-04.2` label.
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion/04.2-all-inbox-beets-preprocessing-with-singleton-auto-promotion-02-SUMMARY.md`
</output>
