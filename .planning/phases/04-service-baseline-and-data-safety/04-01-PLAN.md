---
phase: 04-service-baseline-and-data-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-04-syncthing-contract.sh
  - modules/services/syncthing.nix
autonomous: true
requirements:
  - SRVC-02
  - SRVC-04
must_haves:
  truths:
    - "Operator can enable Syncthing declaratively with explicit folder mode for /srv/data/media."
    - "Syncthing safety controls are explicitly declared to reduce accidental delete/overwrite risk."
    - "Phase verification can fail fast when Syncthing folder mode or safeguards drift."
  artifacts:
    - path: "modules/services/syncthing.nix"
      provides: "Declarative Syncthing service contract with explicit folder mode and versioning safeguards"
    - path: "tests/phase-04-syncthing-contract.sh"
      provides: "Executable contract assertions for SRVC-02 and SRVC-04"
      exports: ["bash contract exit 0"]
  key_links:
    - from: "tests/phase-04-syncthing-contract.sh"
      to: "modules/services/syncthing.nix"
      via: "fixed-string contract assertions"
      pattern: "type = \"sendreceive\""
    - from: "modules/services/syncthing.nix"
      to: "/srv/data/media"
      via: "services.syncthing folder path"
      pattern: "path = \"/srv/data/media\""
---

<objective>
Lock Syncthing service behavior for explicit folder mode and data-safety safeguards on persistent storage.

Purpose: satisfy SRVC-02 and SRVC-04 with enforceable module configuration plus executable drift checks.
Output: phase-04 Syncthing contract test and updated Syncthing module with explicit folder mode/versioning values.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-service-baseline-and-data-safety/04-RESEARCH.md
@.planning/phases/04-service-baseline-and-data-safety/04-VALIDATION.md
@modules/services/syncthing.nix

<interfaces>
Current Syncthing module contract:

```nix
services.syncthing = {
  enable = true;
  dataDir = "/srv/data/media";
  configDir = "/srv/data/syncthing/config";
  openDefaultPorts = false;
};
```

Target explicit folder/safety values to add:

```nix
services.syncthing.settings.folders."media" = {
  path = "/srv/data/media";
  type = "sendreceive";
  versioning = {
    type = "trashcan";
    params = {
      cleanoutDays = "30";
      cleanupIntervalS = "86400";
    };
  };
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing-first Syncthing contract test for mode and safeguards</name>
  <read_first>tests/phase-03-access-contract.sh, tests/phase-03-operations-contract.sh, modules/services/syncthing.nix</read_first>
  <files>tests/phase-04-syncthing-contract.sh</files>
  <behavior>
    - Test fails when Syncthing no longer declares `enable = true;` and `openDefaultPorts = false;`.
    - Test fails when `/srv/data/media` folder mode is not explicitly `sendreceive`.
    - Test fails when Syncthing versioning safeguards are missing or different from trashcan + 30 day cleanout.
  </behavior>
  <action>Create executable `tests/phase-04-syncthing-contract.sh` with `#!/usr/bin/env bash`, `set -euo pipefail`, and `rg --fixed-strings --quiet` assertions against `modules/services/syncthing.nix` for these exact literals: `enable = true;`, `openDefaultPorts = false;`, `settings.folders."media"`, `path = "/srv/data/media";`, `type = "sendreceive";`, `versioning = {`, `type = "trashcan";`, `cleanoutDays = "30";`, and `cleanupIntervalS = "86400";`.</action>
  <acceptance_criteria>`tests/phase-04-syncthing-contract.sh` exists, is executable, and contains all nine fixed-string assertions listed in the action.</acceptance_criteria>
  <verify>
    <automated>bash tests/phase-04-syncthing-contract.sh</automated>
  </verify>
  <done>Syncthing mode/safety contract is enforced by a runnable phase test.</done>
</task>

<task type="auto">
  <name>Task 2: Implement explicit Syncthing folder mode and versioning safeguards</name>
  <read_first>modules/services/syncthing.nix, .planning/phases/04-service-baseline-and-data-safety/04-RESEARCH.md, .planning/phases/04-service-baseline-and-data-safety/04-VALIDATION.md</read_first>
  <files>modules/services/syncthing.nix</files>
  <action>Update `modules/services/syncthing.nix` to keep existing `services.syncthing.enable = true;`, `dataDir = "/srv/data/media";`, `configDir = "/srv/data/syncthing/config";`, and `openDefaultPorts = false;` while adding explicit folder-mode safety contract under `services.syncthing.settings.folders."media"` with exact values: `path = "/srv/data/media";`, `type = "sendreceive";`, `versioning.type = "trashcan";`, `versioning.params.cleanoutDays = "30";`, and `versioning.params.cleanupIntervalS = "86400";`.</action>
  <acceptance_criteria>`modules/services/syncthing.nix` contains `settings.folders."media"` with exact `path`, `type = "sendreceive"`, and trashcan versioning params `cleanoutDays = "30"` + `cleanupIntervalS = "86400"`.</acceptance_criteria>
  <verify>
    <automated>bash tests/phase-04-syncthing-contract.sh</automated>
  </verify>
  <done>Syncthing declarative config explicitly codifies bidirectional mode and versioning safeguards on `/srv/data/media`.</done>
</task>

</tasks>

<verification>
Run `bash tests/phase-04-syncthing-contract.sh` and `just verify-oci-contract`; both must pass before starting plan 04-02.
</verification>

<success_criteria>
- SRVC-02 is enforced by explicit Syncthing folder mode and path declarations.
- SRVC-04 is enforced by explicit versioning safeguard settings and executable drift checks.
- Plan output is ready for service-flow verification in wave 2.
</success_criteria>

<output>
After completion, create `.planning/phases/04-service-baseline-and-data-safety/04-service-baseline-and-data-safety-01-SUMMARY.md`
</output>
