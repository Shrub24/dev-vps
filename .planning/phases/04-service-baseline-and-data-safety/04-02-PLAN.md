---
phase: 04-service-baseline-and-data-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - tests/phase-04-service-flow-contract.sh
  - justfile
  - .planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md
autonomous: true
requirements:
  - SRVC-03
  - SRVC-05
must_haves:
  truths:
    - "Operator can run Navidrome declaratively with media rooted on persistent storage."
    - "Navidrome reads directly from Syncthing-managed /srv/data/media path."
    - "Phase verification fails if duplicate staging/ingest paths are introduced into Navidrome flow."
  artifacts:
    - path: "tests/phase-04-service-flow-contract.sh"
      provides: "Executable service-flow assertions for Navidrome direct-read and no-duplicate-staging"
      exports: ["bash contract exit 0"]
    - path: "justfile"
      provides: "verify-phase-04 command chaining phase-04 checks with baseline contract"
    - path: ".planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md"
      provides: "Operator runbook for direct Syncthing->Navidrome flow and data-safety checks"
  key_links:
    - from: "modules/services/navidrome.nix"
      to: "modules/services/syncthing.nix"
      via: "MusicFolder points to Syncthing-managed path"
      pattern: "MusicFolder = \"/srv/data/media\""
    - from: "justfile"
      to: "tests/phase-04-service-flow-contract.sh"
      via: "verify-phase-04 recipe"
      pattern: "bash tests/phase-04-service-flow-contract.sh"
---

<objective>
Lock the direct Syncthing-to-Navidrome media flow and operator verification path for data-safe baseline operations.

Purpose: satisfy SRVC-03 and SRVC-05 with executable service-flow checks and one phase command for operators.
Output: service-flow contract test, `verify-phase-04` recipe, and phase-04 service-flow runbook.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-service-baseline-and-data-safety/04-RESEARCH.md
@.planning/phases/04-service-baseline-and-data-safety/04-VALIDATION.md
@.planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md
@modules/services/navidrome.nix
@modules/services/syncthing.nix
@justfile

<interfaces>
Current Navidrome direct-read contract:

```nix
services.navidrome.settings = {
  MusicFolder = "/srv/data/media";
  DataFolder = "/srv/data/navidrome";
};
```

Current phase verification aggregation pattern in justfile:

```just
verify-phase-03:
  bash tests/phase-03-bootstrap-contract.sh
  bash tests/phase-03-access-contract.sh
  bash tests/phase-03-operations-contract.sh
  just verify-oci-contract
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add service-flow contract test for direct media path and no duplicate staging</name>
  <read_first>tests/phase-03-operations-contract.sh, modules/services/navidrome.nix, modules/services/syncthing.nix, modules/services/slskd.nix</read_first>
  <files>tests/phase-04-service-flow-contract.sh</files>
  <behavior>
    - Test fails if Navidrome no longer uses `/srv/data/media` as MusicFolder.
    - Test fails if Syncthing no longer uses `/srv/data/media` as the authoritative folder path.
    - Test fails if duplicate staging semantics are introduced to Navidrome flow.
  </behavior>
  <action>Create executable `tests/phase-04-service-flow-contract.sh` using strict mode and fixed-string assertions for exact literals: `MusicFolder = "/srv/data/media";` and `DataFolder = "/srv/data/navidrome";` in `modules/services/navidrome.nix`; `path = "/srv/data/media";` and `type = "sendreceive";` in `modules/services/syncthing.nix`; `/srv/data/inbox/complete` and `/srv/data/inbox/incomplete` in `modules/services/slskd.nix`; and a negative duplicate-path guard that fails if `modules/services/navidrome.nix` contains `/srv/data/inbox` (for example: `if rg --fixed-strings --quiet '/srv/data/inbox' modules/services/navidrome.nix; then echo 'duplicate staging path introduced in navidrome flow'; exit 1; fi`).</action>
  <acceptance_criteria>`tests/phase-04-service-flow-contract.sh` exists, is executable, checks the six positive literals above, and includes the negative guard rejecting `/srv/data/inbox` in `modules/services/navidrome.nix`.</acceptance_criteria>
  <verify>
    <automated>bash tests/phase-04-service-flow-contract.sh</automated>
  </verify>
  <done>Direct-flow and no-duplicate-staging behavior is codified as an executable contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add consolidated verify-phase-04 operator command</name>
  <read_first>justfile, tests/phase-04-syncthing-contract.sh, tests/phase-04-service-flow-contract.sh, .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md</read_first>
  <files>justfile</files>
  <action>Add a new `verify-phase-04` recipe to `justfile` with exact command sequence: `bash tests/phase-04-syncthing-contract.sh`, `bash tests/phase-04-service-flow-contract.sh`, then `just verify-oci-contract`. Keep existing recipes unchanged. Do not replace `verify-phase-03`.</action>
  <acceptance_criteria>`justfile` contains `verify-phase-04:` and includes all three exact commands in the required order.</acceptance_criteria>
  <verify>
    <automated>just verify-phase-04</automated>
  </verify>
  <done>Operator can run one command to validate full phase-04 service baseline contract.</done>
</task>

<task type="auto">
  <name>Task 3: Document phase-04 direct service flow and safety verification steps</name>
  <read_first>.planning/phases/04-service-baseline-and-data-safety/04-RESEARCH.md, .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md, justfile</read_first>
  <files>.planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md</files>
  <action>Create `.planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md` with explicit sections: (1) "Direct media flow contract" stating `Syncthing /srv/data/media (authoritative) -> Navidrome MusicFolder /srv/data/media`; (2) "No duplicate staging rule" stating Navidrome must not read `/srv/data/inbox` and that `/srv/data/inbox` remains slskd-only; (3) "Verification commands" listing `bash tests/phase-04-syncthing-contract.sh`, `bash tests/phase-04-service-flow-contract.sh`, `just verify-phase-04`; and (4) "Operator routine" listing ordered steps `just verify-phase-04`, `just redeploy`, `just status`, `just tailscale-status`.</action>
  <acceptance_criteria>Runbook exists and contains exact strings `Syncthing /srv/data/media (authoritative) -> Navidrome MusicFolder /srv/data/media`, `Navidrome must not read /srv/data/inbox`, `bash tests/phase-04-service-flow-contract.sh`, and `just verify-phase-04`.</acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md && rg "Syncthing /srv/data/media \(authoritative\) -> Navidrome MusicFolder /srv/data/media|Navidrome must not read /srv/data/inbox|bash tests/phase-04-service-flow-contract.sh|just verify-phase-04" .planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md</automated>
  </verify>
  <done>Phase-04 operator guidance mirrors the executable direct-flow safety contract.</done>
</task>

</tasks>

<verification>
Run `just verify-phase-04` and ensure it exits 0 before proceeding to phase execution verification.
</verification>

<success_criteria>
- SRVC-03 is validated by explicit Navidrome media-root checks.
- SRVC-05 is validated by direct Syncthing-to-Navidrome path assertions and duplicate-path rejection.
- Operator has one phase command and one runbook for safe service baseline operations.
</success_criteria>

<output>
After completion, create `.planning/phases/04-service-baseline-and-data-safety/04-service-baseline-and-data-safety-02-SUMMARY.md`
</output>
