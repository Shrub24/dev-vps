---
phase: 04.1-add-beets-inbox-only-singleton-ingestion-phase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-04.1-beets-contract.sh
  - modules/services/beets-inbox.nix
  - modules/applications/music.nix
autonomous: true
requirements:
  - MEDI-02
  - MEDI-03
  - MEDI-04
user_setup:
  - service: beatport
    why: "Authorize the built-in Beatport plugin for Beets metadata lookups"
    dashboard_config:
      - task: "Complete the first interactive Beatport browser/paste-back auth flow from the target host after deploy so tokens persist under /srv/data/beets"
        location: "Beatport OAuth page opened by the Beets runtime"
must_haves:
  truths:
    - "Operator can run Beets as an inbox-only singleton worker against /srv/media/inbox without copying, moving, or linking files out of inbox."
    - "New slskd arrivals can trigger an automatic Beets pass while unmatched or weak-match files stay in inbox and are queued under /srv/data/beets."
    - "Discogs, Beatport, and Bandcamp are available as configured metadata sources, while SoundCloud remains absent."
  artifacts:
    - path: "modules/services/beets-inbox.nix"
      provides: "Inbox-only Beets worker module with generated config, runner, and systemd.path/service wiring"
      exports: ["systemd.paths.beets-inbox-watch", "systemd.services.beets-inbox-run"]
    - path: "modules/applications/music.nix"
      provides: "Application-layer import of the Beets worker and group wiring for inbox writes"
    - path: "tests/phase-04.1-beets-contract.sh"
      provides: "Executable drift checks for inbox-only, metadata-source, and /srv/data/beets contracts"
  key_links:
    - from: "modules/applications/music.nix"
      to: "modules/services/beets-inbox.nix"
      via: "application import"
      pattern: "../../modules/services/beets-inbox.nix"
    - from: "modules/services/beets-inbox.nix"
      to: "/srv/media/inbox/slskd"
      via: "systemd.path watch root"
      pattern: "PathExistsGlob = \"/srv/media/inbox/slskd/*\""
    - from: "modules/services/beets-inbox.nix"
      to: "/srv/data/beets"
      via: "BEETSDIR + generated config/report paths"
      pattern: "BEETSDIR=/srv/data/beets"
---

<objective>
Create the Beets inbox-only worker contract and module wiring for `oci-melb-1`.

Purpose: implement MEDI-02, MEDI-03, and MEDI-04 without changing `/srv/media` authority or introducing promotion behavior.
Output: Beets contract test, reusable `modules/services/beets-inbox.nix`, and music application composition updated to include the worker.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-CONTEXT.md
@.planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-RESEARCH.md
@.planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-VALIDATION.md
@.planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md
@modules/applications/music.nix
@modules/services/slskd.nix
@tests/phase-04-service-flow-contract.sh

<interfaces>
Current music application composition:

```nix
imports = [
  ../../modules/services/syncthing.nix
  ../../modules/services/navidrome.nix
  ../../modules/services/slskd.nix
];

systemd.tmpfiles.rules = [
  "d /srv/media/inbox 2775 root music-ingest - -"
  "f /var/lib/slskd/environment 0640 slskd slskd - -"
];
```

Current slskd inbox path contract:

```nix
services.slskd.settings.directories = {
  downloads = "/srv/media/inbox/slskd";
  incomplete = "/srv/media/slskd/incomplete";
};
```

Execution must preserve the phase-04 service-flow rule:

```text
Navidrome must not read /srv/media/inbox.
/srv/media is the authoritative library path.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Beets inbox-only contract test before module work</name>
  <read_first>tests/phase-04.1-beets-contract.sh, tests/phase-04-service-flow-contract.sh, modules/applications/music.nix, modules/services/slskd.nix</read_first>
  <files>tests/phase-04.1-beets-contract.sh</files>
  <behavior>
    - Test fails if the Beets worker is not imported through `modules/applications/music.nix`.
    - Test fails if the worker no longer watches `/srv/media/inbox/slskd/*`, writes `BEETSDIR=/srv/data/beets`, or disables `copy`, `move`, `link`, and `hardlink`.
    - Test fails if plugin/source literals `discogs`, `beatport`, or `bandcamp` disappear, or if `soundcloud` or any non-inbox promotion path is introduced.
  </behavior>
  <action>Create executable `tests/phase-04.1-beets-contract.sh` with `#!/usr/bin/env bash`, `set -euo pipefail`, and `rg --fixed-strings --quiet` assertions against `modules/services/beets-inbox.nix` and `modules/applications/music.nix`. Encode the exact contract values from D-01 through D-15: import path `../../modules/services/beets-inbox.nix`; `PathExistsGlob = "/srv/media/inbox/slskd/*"`; `BEETSDIR=/srv/data/beets`; YAML/config literals `copy: no`, `move: no`, `link: no`, `hardlink: no`, `write: yes`, `quiet: yes`, `quiet_fallback: skip`; metadata source literals `discogs`, `beatport`, `bandcamp`; report roots `/srv/data/beets/reports` and `/srv/data/beets/unresolved`; and negative guards that fail if `soundcloud` appears or if any literal points imports at `/srv/media` broadly instead of `/srv/media/inbox` / `/srv/media/inbox/slskd`.</action>
  <acceptance_criteria>`tests/phase-04.1-beets-contract.sh` exists, is executable, contains fixed-string checks for `../../modules/services/beets-inbox.nix`, `PathExistsGlob = "/srv/media/inbox/slskd/*"`, `BEETSDIR=/srv/data/beets`, `copy: no`, `move: no`, `link: no`, `hardlink: no`, `discogs`, `beatport`, `bandcamp`, `/srv/data/beets/reports`, and `/srv/data/beets/unresolved`, plus explicit negative guards for `soundcloud` and global `/srv/media` scan drift.</acceptance_criteria>
  <verify>
    <automated>test -x tests/phase-04.1-beets-contract.sh && rg --fixed-strings 'PathExistsGlob = "/srv/media/inbox/slskd/*"' tests/phase-04.1-beets-contract.sh && rg --fixed-strings 'copy: no' tests/phase-04.1-beets-contract.sh && rg --fixed-strings 'soundcloud' tests/phase-04.1-beets-contract.sh</automated>
  </verify>
  <done>The phase has a failing-first executable specification for the Beets inbox-only worker.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the Beets worker module and import it through the music app</name>
  <read_first>modules/services/beets-inbox.nix, modules/applications/music.nix, modules/services/slskd.nix, .planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-CONTEXT.md, .planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-RESEARCH.md, .planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md</read_first>
  <files>modules/services/beets-inbox.nix, modules/applications/music.nix</files>
  <action>Create `modules/services/beets-inbox.nix` and import it from `modules/applications/music.nix`. Implement D-01/D-02/D-03/D-04/D-05/D-07/D-08/D-09/D-10/D-11/D-12/D-13/D-14/D-15 with these concrete values: build a Beets runtime from `pkgs.python3.withPackages` containing `beets`, `requests-oauthlib`, `beetcamp`, and `"discogs-client"`; create a system user/group `beets` with `extraGroups = [ "music-ingest" "music-library" ]`; create `/srv/data/beets`, `/srv/data/beets/state`, `/srv/data/beets/reports`, and `/srv/data/beets/unresolved` via `systemd.tmpfiles.rules`; generate `/srv/data/beets/config.yaml` from Nix with YAML lines `directory: /srv/media/inbox`, `library: /srv/data/beets/state/library.db`, `plugins: discogs beatport bandcamp fromfilename`, and `import:` containing `write: yes`, `copy: no`, `move: no`, `link: no`, `hardlink: no`, `quiet: yes`, `quiet_fallback: skip`, `incremental: no`, `incremental_skip_later: no`; expose a repo-owned shell command `beets-inbox-runner` that defaults to `/srv/media/inbox/slskd`, accepts an explicit subpath argument under `/srv/media/inbox`, skips files newer than 120 seconds, writes timestamped summary files to `/srv/data/beets/reports/<timestamp>-summary.txt`, writes unresolved items to `/srv/data/beets/unresolved/<timestamp>-unresolved.txt`, invokes `beet -c /srv/data/beets/config.yaml import -s -C` for normal runs, and appends `-p` when `BEETS_DRY_RUN=1`; wire `systemd.services.beets-inbox-run` as `Type = "oneshot"` running that command as `beets`; wire `systemd.paths.beets-inbox-watch.pathConfig.PathExistsGlob = "/srv/media/inbox/slskd/*"` so file arrivals are the primary mode per D-05/D-06. Do not add copy/move/link/hardlink behavior, do not point imports at `/srv/media` globally, do not store artifacts under `/srv/media`, and do not add SoundCloud.</action>
  <acceptance_criteria>`modules/services/beets-inbox.nix` exists and contains `PathExistsGlob = "/srv/media/inbox/slskd/*"`, `Type = "oneshot"`, `BEETSDIR=/srv/data/beets`, `/srv/data/beets/reports`, `/srv/data/beets/unresolved`, `copy: no`, `move: no`, `link: no`, `hardlink: no`, `plugins: discogs beatport bandcamp fromfilename`, and `beet -c /srv/data/beets/config.yaml import -s -C`; `modules/applications/music.nix` imports `../../modules/services/beets-inbox.nix` and keeps `/srv/media/inbox` owned by `music-ingest`.</acceptance_criteria>
  <verify>
    <automated>bash tests/phase-04.1-beets-contract.sh</automated>
  </verify>
  <done>The repository has a declarative Beets worker that tags inbox files in place, auto-triggers from slskd arrivals, and keeps all persistent state under `/srv/data/beets`.</done>
</task>

</tasks>

<verification>
Run `bash tests/phase-04.1-beets-contract.sh` and `just verify-phase-04`; both must exit 0 before wave 2 starts.
</verification>

<success_criteria>
- MEDI-02 is satisfied by an inbox-only singleton worker that writes tags in place and never promotes files out of inbox.
- MEDI-03 is satisfied by automatic file-event-driven execution from `/srv/media/inbox/slskd` plus structured unresolved/report output under `/srv/data/beets`.
- MEDI-04 is satisfied by explicit Discogs, Beatport, and Bandcamp source wiring with conservative skip behavior for weak or unmatched files.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-add-beets-inbox-only-singleton-ingestion-phase-01-SUMMARY.md`
</output>
