---
phase: 04.1-add-beets-inbox-only-singleton-ingestion-phase
plan: 02
type: execute
wave: 2
depends_on:
  - "04.1-01"
files_modified:
  - justfile
  - .planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-BEETS-WORKER.md
  - docs/decisions.md
autonomous: true
requirements:
  - MEDI-03
must_haves:
  truths:
    - "Operator can verify phase 04.1 with one command that re-runs the phase-04 baseline checks plus the new Beets contract."
    - "Operator can follow one runbook for automatic flow, dry-run validation, and unresolved/report locations without rediscovering module internals."
    - "Canonical decisions record that Beets is inbox-only and non-promoting so future changes cannot silently turn it into a library mover."
  artifacts:
    - path: "justfile"
      provides: "verify-phase-04.1 operator recipe"
    - path: ".planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-BEETS-WORKER.md"
      provides: "Phase runbook for Beets worker operation, dry-run use, and report review"
    - path: "docs/decisions.md"
      provides: "Canonical decision entry for inbox-only Beets behavior and boundaries"
  key_links:
    - from: "justfile"
      to: "tests/phase-04.1-beets-contract.sh"
      via: "verify-phase-04.1 recipe"
      pattern: "bash tests/phase-04.1-beets-contract.sh"
    - from: "justfile"
      to: "verify-phase-04"
      via: "phase aggregation"
      pattern: "just verify-phase-04"
    - from: ".planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-BEETS-WORKER.md"
      to: "beets-inbox-runner"
      via: "documented command surface"
      pattern: "beets-inbox-runner /srv/media/inbox/slskd"
---

<objective>
Add the operator verification and documentation layer for the Beets inbox-only worker.

Purpose: make MEDI-03 operationally verifiable and keep the new inbox-only boundary explicit in the canonical docs.
Output: `verify-phase-04.1` recipe, Beets worker runbook, and decision register entry for the new boundary.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-CONTEXT.md
@.planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-RESEARCH.md
@.planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-VALIDATION.md
@.planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md
@justfile
@docs/decisions.md

<interfaces>
Existing phase aggregation pattern:

```just
verify-phase-04:
  bash tests/phase-04-syncthing-contract.sh
  bash tests/phase-04-service-flow-contract.sh
  just verify-oci-contract
```

Planned command surface from 04.1-01:

```text
beets-inbox-runner /srv/media/inbox/slskd
BEETS_DRY_RUN=1 beets-inbox-runner /srv/media/inbox/slskd
beets-inbox-runner /srv/media/inbox/<selected-subpath>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a single phase verification recipe for the Beets worker</name>
  <read_first>justfile, tests/phase-04-syncthing-contract.sh, tests/phase-04-service-flow-contract.sh, tests/phase-04.1-beets-contract.sh</read_first>
  <files>justfile</files>
  <action>Add `verify-phase-04.1` to `justfile` with this exact command sequence: first `just verify-phase-04`, then `bash tests/phase-04.1-beets-contract.sh`. Keep `verify-phase-04` unchanged so phase 04 remains independently runnable. This makes the existing phase-04 baseline the precondition and layers the Beets-specific drift checks on top.</action>
  <acceptance_criteria>`justfile` contains a `verify-phase-04.1:` recipe with the exact lines `just verify-phase-04` and `bash tests/phase-04.1-beets-contract.sh` in that order.</acceptance_criteria>
  <verify>
    <automated>just verify-phase-04.1</automated>
  </verify>
  <done>Operators can verify the full media baseline plus Beets worker with one command.</done>
</task>

<task type="auto">
  <name>Task 2: Document the Beets worker operating contract and record the new boundary decision</name>
  <read_first>.planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-BEETS-WORKER.md, .planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-CONTEXT.md, .planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-RESEARCH.md, .planning/phases/04-service-baseline-and-data-safety/04-SERVICE-FLOW.md, docs/decisions.md</read_first>
  <files>.planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-BEETS-WORKER.md, docs/decisions.md</files>
  <action>Create `.planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-BEETS-WORKER.md` with concrete sections and exact strings: `Automatic flow` showing `/srv/media/inbox/slskd -> beets-inbox-watch.path -> beets-inbox-run.service -> in-place tag writes in inbox -> /srv/data/beets/reports + /srv/data/beets/unresolved`; `Manual targeting` listing `beets-inbox-runner /srv/media/inbox/slskd`, `BEETS_DRY_RUN=1 beets-inbox-runner /srv/media/inbox/slskd`, and `beets-inbox-runner /srv/media/inbox/<selected-subpath>`; `Verification` listing `bash tests/phase-04.1-beets-contract.sh` and `just verify-phase-04.1`; `Beatport authorization` noting the first interactive browser/paste-back flow stores tokens under `/srv/data/beets`; and `Guardrails` stating `copy/move/link/hardlink remain disabled`, `global /srv/media scans are forbidden`, and `SoundCloud stays deferred`. Also append a new `docs/decisions.md` entry after D-020 that records the same boundary in canonical form: inbox-only singleton Beets worker, automatic `systemd.path` trigger, state under `/srv/data/beets`, and no promotion behavior.</action>
  <acceptance_criteria>The runbook contains the exact strings `beets-inbox-runner /srv/media/inbox/slskd`, `BEETS_DRY_RUN=1 beets-inbox-runner /srv/media/inbox/slskd`, `just verify-phase-04.1`, `/srv/data/beets/reports`, and `SoundCloud stays deferred`; `docs/decisions.md` contains a new Beets decision entry mentioning `inbox-only`, `/srv/media/inbox/slskd`, `/srv/data/beets`, and `no promotion behavior`.</acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-BEETS-WORKER.md && rg "beets-inbox-runner /srv/media/inbox/slskd|BEETS_DRY_RUN=1 beets-inbox-runner /srv/media/inbox/slskd|just verify-phase-04.1|/srv/data/beets/reports|SoundCloud stays deferred" .planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-BEETS-WORKER.md && rg "inbox-only|/srv/media/inbox/slskd|/srv/data/beets|no promotion behavior" docs/decisions.md</automated>
  </verify>
  <done>The new Beets worker is documented as an inbox-only, non-promoting service with explicit operator commands and canonical decision coverage.</done>
</task>

</tasks>

<verification>
Run `just verify-phase-04.1` and confirm the runbook/decision text matches the implemented command surface before closing the phase.
</verification>

<success_criteria>
- MEDI-03 remains operationally enforceable through a single verification command and a documented automatic flow.
- The operator has exact manual and dry-run commands for inbox-scoped execution only.
- Canonical docs make the Beets worker boundary explicit enough to prevent future accidental promotion or SoundCloud creep.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-add-beets-inbox-only-singleton-ingestion-phase/04.1-add-beets-inbox-only-singleton-ingestion-phase-02-SUMMARY.md`
</output>
