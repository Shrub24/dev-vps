# Phase 04.1: Add Beets inbox-only singleton ingestion phase - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md - this log preserves alternatives considered.

**Date:** 2026-03-29
**Phase:** 04.1-add-beets-inbox-only-singleton-ingestion-phase
**Areas discussed:** Tag-write policy, Run model and cadence, Metadata source policy, Output and audit trail

---

## Tag-write policy

| Option | Description | Selected |
|--------|-------------|----------|
| Write tags only | Update metadata in place in `/srv/media/inbox`, no copy/move/link during import | yes |
| Write + copy | Copy tagged files into another path during import | |
| Write + move | Move files out of inbox during import | |

**User's choice:** Write tags only.
**Notes:** Inbox-only policy remains strict for this phase.

| Option | Description | Selected |
|--------|-------------|----------|
| Keep untouched | Leave low-confidence/no-match files unchanged and log for follow-up | yes |
| Import as-is | Record in DB without tag changes | |
| Force best guess | Always apply closest match metadata | |

**User's choice:** Keep untouched.
**Notes:** Safety preferred over aggressive automation.

| Option | Description | Selected |
|--------|-------------|----------|
| Conservative merge | Replace tags only when matched metadata is clearly better | yes |
| Always overwrite | Replace existing tags when a match is selected | |
| Preserve existing | Fill only missing fields | |

**User's choice:** Conservative merge.

| Option | Description | Selected |
|--------|-------------|----------|
| Singleton-first | Default flow treats inbox files as individual tracks | yes |
| Album-first | Prefer album grouping/matching first | |
| Prompt each run | Choose singleton vs album each invocation | |

**User's choice:** Singleton-first.

---

## Run model and cadence

| Option | Description | Selected |
|--------|-------------|----------|
| Manual command runs | Operator starts each run explicitly | |
| Assisted wrapper flow | Guided invocation path with confirmations | |
| Automatic on schedule | Unattended timer-based runs | |
| Custom: file-watch worker | Automatic runs on new `slskd` downloads via file watch | yes |

**User's choice:** Custom - file-watch worker model.
**Notes:** Manual execution acceptable only for initial seeding/development.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, dry-run first | Dry-run before write-enabled path | yes* |
| Optional only | Dry-run exists but not default operational requirement | |
| No dry-run path | Write directly with no dry-run flow | |

**User's choice:** Yes, with qualification.
**Notes:** Dry-run is for development purposes only, not required for all steady-state runs.

| Option | Description | Selected |
|--------|-------------|----------|
| justfile recipes | Add dedicated day-2 command surface to just | |
| Direct beet commands only | Shell command usage without additional wrappers | |
| Dedicated shell script | New script entrypoint | |
| Custom: worker service | Module runs as worker; just remains deploy/manage only | yes |

**User's choice:** Custom - worker service model.

| Option | Description | Selected |
|--------|-------------|----------|
| Inbox path explicit each run | Target `/srv/media/inbox` explicitly | yes |
| Global beets library scan | Broad default scan roots | |
| Per-subfolder only | Require deeper path each run | |

**User's choice:** Inbox path explicit each run.

---

## Metadata source policy

| Option | Description | Selected |
|--------|-------------|----------|
| Discogs + Beatport enabled | Enable both as metadata sources | yes |
| Discogs only | Discogs as sole non-MusicBrainz source | |
| MusicBrainz-only fallback | Do not enable Discogs/Beatport plugins | |

**User's choice:** Discogs + Beatport.

| Option | Description | Selected |
|--------|-------------|----------|
| Best-score wins | Accept clearer match based on scoring | yes |
| Prefer Discogs first | Hard source precedence to Discogs | |
| Prefer Beatport first | Hard source precedence to Beatport | |

**User's choice:** Best-score wins.

| Option | Description | Selected |
|--------|-------------|----------|
| Structured unresolved queue | Persistent unresolved reporting under `/srv/data/beets` | yes |
| Console output only | No persistent unresolved report | |
| Sidecar marker files | Marker files near inbox tracks | |

**User's choice:** Structured unresolved queue.

| Option | Description | Selected |
|--------|-------------|----------|
| Defer both | Bandcamp and SoundCloud both deferred | |
| Bandcamp now, SoundCloud deferred | Include Bandcamp in 04.1 only | yes |
| Include both now | Broad source expansion | |

**User's choice:** Bandcamp now, SoundCloud deferred.

---

## Output and audit trail

| Option | Description | Selected |
|--------|-------------|----------|
| Info-level summaries | Concise per-run operational summaries | yes |
| Verbose per-track logs | Detailed item-level logs on each run | |
| Minimal success-only | Emit mostly on failures | |

**User's choice:** Info-level summaries.

| Option | Description | Selected |
|--------|-------------|----------|
| Only `/srv/data/beets` | Keep logs/reports/state on service-state mount | yes |
| Both `/srv/data` and inbox | Duplicate artifacts near media files | |
| Inbox only | Store reports in media tree | |

**User's choice:** Only `/srv/data/beets`.

| Option | Description | Selected |
|--------|-------------|----------|
| Timestamped run reports | Per-run report files with timestamps | yes |
| Single rolling file | One unresolved file overwritten each run | |
| Database only | No human-readable report files | |

**User's choice:** Timestamped run reports.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, concise change report | Include successful tag updates each run | yes |
| No, unresolved-only report | Only track failures/unmatched | |
| Debug-mode only | Success reports only in debug mode | |

**User's choice:** Yes, concise change report.

---

## the agent's Discretion

- Exact implementation of file-watch trigger mechanism.
- Exact report file format/schema under `/srv/data/beets`.
- Exact service unit decomposition and dependency wiring.

## Deferred Ideas

- SoundCloud integration deferred beyond this phase.
- Promotion/move pipeline from inbox into curated library deferred to a future phase.
