---
phase: 01-repository-cutover
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - justfile
  - deploy.sh
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - REPO-01
  - REPO-03
must_haves:
  truths:
    - "Operator command surface targets oci-melb-1 instead of dev-vps/droplet naming"
    - "CI validates the same canonical host output used by operators"
    - "Install and rebuild commands reference the fleet cutover flake target"
  artifacts:
    - path: "justfile"
      provides: "Operator commands aligned to canonical host"
    - path: "deploy.sh"
      provides: "Bootstrap command aligned to canonical host"
    - path: ".github/workflows/ci.yml"
      provides: "CI checks aligned to canonical host output"
  key_links:
    - from: "justfile"
      to: "flake.nix"
      via: "--flake path:.#oci-melb-1"
      pattern: "flake path:\.#oci-melb-1"
    - from: "deploy.sh"
      to: "flake.nix"
      via: "--flake path:.#oci-melb-1"
      pattern: "--flake \"path:\.#oci-melb-1\""
    - from: ".github/workflows/ci.yml"
      to: "flake.nix"
      via: "nix build target"
      pattern: "nixosConfigurations.oci-melb-1"
---

<objective>
Align operator and CI command surfaces with the new canonical host target and remove legacy `dev-vps`/droplet assumptions.

Purpose: complete operational cutover so daily usage and automation match the new repository shape (D-01, D-05, D-07).
Output: `justfile`, `deploy.sh`, and CI workflow all targeting `oci-melb-1`.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-cutover/01-CONTEXT.md
@.planning/phases/01-repository-cutover/01-RESEARCH.md
@flake.nix
@justfile
@deploy.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update operator commands from droplet/dev-vps to host-centric target</name>
  <read_first>justfile, deploy.sh, flake.nix, .planning/phases/01-repository-cutover/01-CONTEXT.md</read_first>
  <files>justfile, deploy.sh</files>
  <action>In `justfile`, replace `DROPLET_IP` and `DROPLET_USER` with neutral names `TARGET_HOST` and `TARGET_USER` (D-05). Update all recipes that call `--flake path:.#dev-vps` or `nixosConfigurations.dev-vps` to use `oci-melb-1` (`--flake path:.#oci-melb-1` and `nixosConfigurations.oci-melb-1`). Keep SSH/status recipes functional but remove personal-tooling unit defaults like `codenomad`/`tailscale-serve-codenomad` from status checks (D-08). In `deploy.sh`, rename usage/help text from droplet language to host language and set flake target to `path:.#oci-melb-1`.</action>
  <acceptance_criteria>`justfile` contains `TARGET_HOST` and `TARGET_USER`; `justfile` has no `DROPLET_IP`, `DROPLET_USER`, `dev-vps`, `codenomad`, or `tailscale-serve-codenomad`; `deploy.sh` contains `path:.#oci-melb-1` and no `dev-vps` or `droplet` strings.</acceptance_criteria>
  <verify>
    <automated>! rg -q "DROPLET_IP|DROPLET_USER|dev-vps|codenomad|tailscale-serve-codenomad" justfile deploy.sh && rg "TARGET_HOST|TARGET_USER|path:.#oci-melb-1" justfile deploy.sh</automated>
  </verify>
  <done>Operator-facing commands are host-centric and no longer embed legacy naming/personal baseline assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Align CI checks with canonical host output</name>
  <read_first>.github/workflows/ci.yml, flake.nix</read_first>
  <files>.github/workflows/ci.yml</files>
  <action>Update `.github/workflows/ci.yml` to build only active outputs that exist after cutover. Replace `nixosConfigurations.dev-vps.config.system.build.toplevel` with `nixosConfigurations.oci-melb-1.config.system.build.toplevel`. Remove pinned package build steps for `packages.x86_64-linux.codenomad`, `packages.x86_64-linux.opencode`, and `packages.x86_64-linux.repo-sync` because those legacy outputs are removed in Plan 01 (D-07, D-08). Keep `nix flake check --no-build` as the first validation step.</action>
  <acceptance_criteria>CI workflow contains `nixosConfigurations.oci-melb-1.config.system.build.toplevel`; CI workflow does not contain `nixosConfigurations.dev-vps` or `packages.x86_64-linux.codenomad|opencode|repo-sync`; CI still contains `nix flake check --no-build`.</acceptance_criteria>
  <verify>
    <automated>rg "nix flake check --no-build|nixosConfigurations.oci-melb-1.config.system.build.toplevel" .github/workflows/ci.yml && ! rg -q "nixosConfigurations.dev-vps|packages.x86_64-linux.codenomad|packages.x86_64-linux.opencode|packages.x86_64-linux.repo-sync" .github/workflows/ci.yml</automated>
  </verify>
  <done>Automation validates the same canonical host path operators use.</done>
</task>

</tasks>

<verification>
Run `nix flake check --no-build --no-write-lock-file path:.` and `nix build --no-link --no-write-lock-file path:.#nixosConfigurations.oci-melb-1.config.system.build.toplevel`.
</verification>

<success_criteria>
- Operator commands and CI are free of `dev-vps` and droplet-centric naming.
- Commands target the same host output from `flake.nix`.
- Requirement IDs REPO-01 and REPO-03 are reinforced operationally.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-cutover/01-repository-cutover-02-SUMMARY.md`
</output>
