---
phase: 01-repository-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flake.nix
  - hosts/oci-melb-1/default.nix
  - modules/core/base.nix
  - modules/profiles/base-server.nix
  - modules/services/tailscale.nix
autonomous: true
requirements:
  - REPO-01
  - REPO-03
must_haves:
  truths:
    - "Operator can build a host named oci-melb-1 from the canonical flake entrypoint"
    - "Repository has distinct host identity and reusable module boundaries"
    - "Legacy missing references no longer break flake evaluation"
  artifacts:
    - path: "hosts/oci-melb-1/default.nix"
      provides: "Host identity entrypoint"
    - path: "modules/core/base.nix"
      provides: "Reusable core baseline module"
    - path: "modules/profiles/base-server.nix"
      provides: "Reusable profile composition"
    - path: "modules/services/tailscale.nix"
      provides: "Reusable service module boundary"
    - path: "flake.nix"
      provides: "Canonical host assembly and output target"
  key_links:
    - from: "flake.nix"
      to: "hosts/oci-melb-1/default.nix"
      via: "imports in nixosConfigurations.oci-melb-1"
      pattern: "nixosConfigurations\\.oci-melb-1"
    - from: "hosts/oci-melb-1/default.nix"
      to: "modules/core/base.nix"
      via: "imports"
      pattern: "\\.\\./\\.\\./modules/core/base\\.nix"
    - from: "hosts/oci-melb-1/default.nix"
      to: "modules/profiles/base-server.nix"
      via: "imports"
      pattern: "\\.\\./\\.\\./modules/profiles/base-server\\.nix"
---

<objective>
Create the canonical host-plus-modules cutover skeleton and rewire `flake.nix` to that structure.

Purpose: satisfy the core repository cutover decisions (D-01, D-02, D-03, D-04, D-07, D-08) and eliminate broken legacy references.
Output: fleet-oriented host/module files and a buildable `nixosConfigurations.oci-melb-1` entrypoint.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-cutover/01-CONTEXT.md
@.planning/phases/01-repository-cutover/01-RESEARCH.md
@docs/architecture.md
@flake.nix

<interfaces>
From `flake.nix` (current target to replace):

```nix
nixosConfigurations.dev-vps = nixpkgs.lib.nixosSystem {
  inherit system;
  modules = [ ... ./nixos/configuration.nix ];
};
```

Target contract this plan must establish:

```nix
nixosConfigurations.oci-melb-1 = nixpkgs.lib.nixosSystem {
  system = "aarch64-linux";
  modules = [ ./hosts/oci-melb-1/default.nix ... ];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create host-and-module contract files for cutover</name>
  <read_first>flake.nix, nixos/configuration.nix, docs/architecture.md, .planning/phases/01-repository-cutover/01-CONTEXT.md</read_first>
  <files>hosts/oci-melb-1/default.nix, modules/core/base.nix, modules/profiles/base-server.nix, modules/services/tailscale.nix</files>
  <action>Create `hosts/oci-melb-1/default.nix` importing exactly `../../modules/core/base.nix`, `../../modules/profiles/base-server.nix`, and `../../modules/services/tailscale.nix` (D-02, D-03). Set `networking.hostName = "oci-melb-1"` and `system.stateVersion = "25.11"` in the host file. In `modules/core/base.nix`, define shared baseline settings moved from current host baseline (Nix features and SSH policy that is not provider-specific). In `modules/profiles/base-server.nix`, keep profile composition thin and import core baseline only (D-04). In `modules/services/tailscale.nix`, expose a reusable module boundary that enables `services.tailscale` without personal-tooling concerns (D-08). Do not create additional placeholder module files beyond these wired paths (D-04).</action>
  <acceptance_criteria>Files exist at the four exact paths; `hosts/oci-melb-1/default.nix` contains `networking.hostName = "oci-melb-1"`; host file imports all three module paths; no new unused placeholder modules are created outside listed files.</acceptance_criteria>
  <verify>
    <automated>rg "oci-melb-1|modules/core/base.nix|modules/profiles/base-server.nix|modules/services/tailscale.nix" hosts/oci-melb-1/default.nix && test -f modules/core/base.nix && test -f modules/profiles/base-server.nix && test -f modules/services/tailscale.nix</automated>
  </verify>
  <done>Canonical host identity and reusable module boundaries exist and are wired together per D-02/D-03/D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire flake to canonical host and remove broken legacy refs</name>
  <read_first>flake.nix, hosts/oci-melb-1/default.nix, .planning/phases/01-repository-cutover/01-CONTEXT.md</read_first>
  <files>flake.nix</files>
  <action>Update `flake.nix` so active system target is `aarch64-linux` and NixOS output is named `nixosConfigurations.oci-melb-1` (D-01, D-02). Remove overlay/package outputs for `codenomad`, `opencode`, and `repo-sync`, remove Home Manager wiring to `./home/dev.nix`, and remove legacy provider-specific import `./nixos/digitalocean.nix` from active host composition (D-05, D-07, D-08). Keep only modules required for the canonical baseline to evaluate: `disko`, `sops-nix`, and `./hosts/oci-melb-1/default.nix`.</action>
  <acceptance_criteria>`flake.nix` contains `nixosConfigurations.oci-melb-1`; `flake.nix` does not contain `home/dev.nix`; `flake.nix` does not contain `pkgs/codenomad`, `pkgs/opencode`, or `pkgs/repo-sync`; `flake.nix` does not contain `nixosConfigurations.dev-vps`.</acceptance_criteria>
  <verify>
    <automated>rg "nixosConfigurations.oci-melb-1" flake.nix && ! rg -q "home/dev.nix|pkgs/codenomad|pkgs/opencode|pkgs/repo-sync|nixosConfigurations.dev-vps" flake.nix && nix flake check --no-build --no-write-lock-file path:.</automated>
  </verify>
  <done>Flake entrypoint is fleet-oriented, uses canonical host identity, and no longer depends on broken legacy references.</done>
</task>

</tasks>

<verification>
Run `nix flake check --no-build --no-write-lock-file path:.` and confirm output target `nixosConfigurations.oci-melb-1` evaluates.
</verification>

<success_criteria>
- `flake.nix` builds around `hosts/oci-melb-1` and `modules/*` boundaries.
- No broken legacy missing-file references remain in active flake wiring.
- Requirement IDs REPO-01 and REPO-03 are directly advanced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-cutover/01-repository-cutover-01-SUMMARY.md`
</output>
