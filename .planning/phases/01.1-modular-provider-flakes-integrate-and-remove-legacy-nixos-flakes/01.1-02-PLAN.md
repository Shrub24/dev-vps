---
phase: 01.1-modular-provider-flakes-integrate-and-remove-legacy-nixos-flakes
plan: 02
type: execute
wave: 2
depends_on:
  - 01.1-01
files_modified:
  - nixos/configuration.nix
  - nixos/digitalocean.nix
  - nixos/disko-config.nix
  - docs/architecture.md
  - docs/context-history.md
autonomous: true
requirements:
  - REPO-02
  - OPER-02
must_haves:
  truths:
    - "Repository no longer carries stale active-looking NixOS legacy files under nixos/"
    - "Operators can trust docs to point at active host/module paths only"
    - "Code and docs agree on provider boundary location"
  artifacts:
    - path: "nixos/configuration.nix"
      provides: "Removed obsolete legacy host baseline"
    - path: "nixos/digitalocean.nix"
      provides: "Removed obsolete provider-specific legacy module"
    - path: "nixos/disko-config.nix"
      provides: "Removed moved legacy disko path"
    - path: "docs/architecture.md"
      provides: "Canonical architecture with active provider/storage paths"
    - path: "docs/context-history.md"
      provides: "Canonical migration history reflecting completed legacy retirement"
  key_links:
    - from: "docs/architecture.md"
      to: "hosts/oci-melb-1/default.nix"
      via: "active path references"
      pattern: "hosts/oci-melb-1/default.nix"
    - from: "docs/architecture.md"
      to: "modules/providers/oci/default.nix"
      via: "provider boundary references"
      pattern: "modules/providers/oci/default.nix"
    - from: "docs/context-history.md"
      to: "modules/storage/disko-root.nix"
      via: "current truth snapshot"
      pattern: "modules/storage/disko-root.nix"
---

<objective>
Retire the obsolete legacy `nixos/` implementation files and reconcile canonical docs to the active provider-modular flake structure.

Purpose: remove lingering dual-mission ambiguity and lock docs to the post-cutover source of truth.
Output: deleted legacy NixOS files and docs that reference only active architecture paths.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-cutover/01-repository-cutover-03-SUMMARY.md
@.planning/phases/01.1-modular-provider-flakes-integrate-and-remove-legacy-nixos-flakes/01.1-RESEARCH.md
@flake.nix
@docs/architecture.md
@docs/context-history.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove obsolete legacy nixos files after active wiring migration</name>
  <read_first>flake.nix, hosts/oci-melb-1/default.nix, nixos/configuration.nix, nixos/digitalocean.nix, nixos/disko-config.nix, .planning/phases/01.1-modular-provider-flakes-integrate-and-remove-legacy-nixos-flakes/01.1-01-PLAN.md</read_first>
  <files>nixos/configuration.nix, nixos/digitalocean.nix, nixos/disko-config.nix</files>
  <action>Delete `nixos/configuration.nix`, `nixos/digitalocean.nix`, and `nixos/disko-config.nix` after Plan 01 migration is complete. Ensure no active path in `flake.nix`, `hosts/oci-melb-1/default.nix`, `deploy.sh`, `justfile`, or `.github/workflows/ci.yml` references those files. Keep the `nixos/` directory only if it is empty or needed for future intentional work; do not leave deleted-file references in code or scripts.</action>
  <acceptance_criteria>The three legacy files are absent; grep across flake/host/ops/CI files finds no `nixos/configuration.nix`, `nixos/digitalocean.nix`, or `nixos/disko-config.nix` references; `nix flake check --no-build --no-write-lock-file path:.` passes.</acceptance_criteria>
  <verify>
    <automated>test ! -f nixos/configuration.nix && test ! -f nixos/digitalocean.nix && test ! -f nixos/disko-config.nix && ! rg -q "nixos/configuration.nix|nixos/digitalocean.nix|nixos/disko-config.nix" flake.nix hosts/oci-melb-1/default.nix deploy.sh justfile .github/workflows/ci.yml && nix flake check --no-build --no-write-lock-file path:.</automated>
  </verify>
  <done>Legacy `nixos/` implementation files are retired and no active wiring points at them.</done>
</task>

<task type="auto">
  <name>Task 2: Update canonical docs to match provider-modular active paths</name>
  <read_first>docs/architecture.md, docs/context-history.md, hosts/oci-melb-1/default.nix, modules/providers/oci/default.nix, modules/storage/disko-root.nix</read_first>
  <files>docs/architecture.md, docs/context-history.md</files>
  <action>Update `docs/architecture.md` and `docs/context-history.md` so active architecture references include `hosts/oci-melb-1/default.nix`, `modules/providers/oci/default.nix`, and `modules/storage/disko-root.nix`. Remove any wording that implies legacy `nixos/configuration.nix` or `nixos/digitalocean.nix` are active baseline paths. Keep migration intent language consistent with aggressive cleanup and canonical docs authority established in prior phase documentation decisions.</action>
  <acceptance_criteria>Both docs contain the three active path strings; docs do not describe `nixos/configuration.nix` or `nixos/digitalocean.nix` as active architecture; docs still communicate canonical-docs authority and migration continuity.</acceptance_criteria>
  <verify>
    <automated>rg "hosts/oci-melb-1/default.nix|modules/providers/oci/default.nix|modules/storage/disko-root.nix" docs/architecture.md docs/context-history.md && ! rg -q "active.*nixos/configuration.nix|active.*nixos/digitalocean.nix" docs/architecture.md docs/context-history.md</automated>
  </verify>
  <done>Canonical docs and active implementation paths are aligned after legacy file retirement.</done>
</task>

</tasks>

<verification>
Run grep-based legacy-reference checks and full `nix flake check` after file deletion and docs updates.
</verification>

<success_criteria>
- No active references to removed legacy `nixos/*.nix` files remain.
- Canonical docs reflect provider/storage modules in active tree.
- Repository no longer presents dual active architecture surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-modular-provider-flakes-integrate-and-remove-legacy-nixos-flakes/01.1-modular-provider-flakes-integrate-and-remove-legacy-nixos-flakes-02-SUMMARY.md`
</output>
