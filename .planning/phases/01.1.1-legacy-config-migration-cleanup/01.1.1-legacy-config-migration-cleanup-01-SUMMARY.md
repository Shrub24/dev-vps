---
phase: 01.1.1-legacy-config-migration-cleanup
plan: 01
subsystem: infra
tags: [nixos, sops, justfile, secrets]
requires:
  - phase: 01.1
    provides: active host/module architecture and legacy nixos retirement
provides:
  - Operator recipes use active tailscale service naming
  - Common secret template scaffold and migration-safe SOPS path rules
affects: [phase-02-secrets-and-bootstrap, operator-runbooks]
tech-stack:
  added: []
  patterns: [common-and-host-secret-path-scoping, active-service-default-logs]
key-files:
  created: [secrets/common.template.yaml]
  modified: [justfile, .sops.yaml, secrets/secrets.template.yaml]
key-decisions:
  - "Set just logs default to tailscaled to remove retired codenomad assumptions from operator commands."
  - "Adopt transitional SOPS regex covering both secrets/common.yaml and legacy secrets/secrets.yaml while adding host-scoped rule."
patterns-established:
  - "Secret scaffolding: common template at secrets/common.template.yaml with host-specific secrets under hosts/<host>/secrets.yaml"
requirements-completed: [REPO-02]
duration: 8min
completed: 2026-03-21
---

# Phase 01.1.1 Plan 01: Legacy Config Migration Cleanup Summary

**Operator defaults now target tailscaled and secret scaffolding moved to common-template naming with migration-safe SOPS path rules.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-21T04:30:00Z
- **Completed:** 2026-03-21T04:38:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Updated `justfile` `logs` default to `tailscaled` and removed retired workload default usage.
- Added `secrets/common.template.yaml` as the canonical unencrypted common secret template.
- Updated `.sops.yaml` to explicitly support transitional `secrets/(common|secrets).yaml` paths and host-scoped `hosts/<host>/secrets.yaml` paths.
- Removed legacy `secrets/secrets.template.yaml` template naming from active scaffolding.

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove retired workload defaults from operator recipes** - `b8b4232` (fix)
2. **Task 2: Migrate legacy secrets template naming to common-scaffold contract** - `93b7107` (feat)

## Files Created/Modified
- `justfile` - changed logs default service unit from retired value to active `tailscaled`.
- `.sops.yaml` - narrowed common-secret rule and added explicit host-scoped secret rule.
- `secrets/common.template.yaml` - added canonical common secret template placeholders.
- `secrets/secrets.template.yaml` - removed retired monolithic template.

## Decisions Made
- Keep one transitional SOPS regex for `secrets/common.yaml` and `secrets/secrets.yaml` to avoid breaking current encrypted file naming during migration.
- Introduce a separate host-path rule now (`hosts/<host>/secrets.yaml`) to preserve blast-radius boundaries before Phase 2 rollout.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Known Stubs

- `secrets/common.template.yaml:5` and `secrets/common.template.yaml:8` include `REPLACE_WITH_*` placeholders intentionally as unencrypted template guidance.

## Next Phase Readiness

- Canonical secret naming and operator defaults are aligned for docs/codebase map reconciliation in Plan 01.1.1-02.

## Self-Check: PASSED

---
*Phase: 01.1.1-legacy-config-migration-cleanup*
*Completed: 2026-03-21*
