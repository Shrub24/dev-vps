---
phase: 01.1.1-legacy-config-migration-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 01.1.1-01
files_modified:
  - docs/architecture.md
  - docs/plan.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements:
  - REPO-02
  - OPER-02
must_haves:
  truths:
    - "Canonical docs describe current secret scaffolding and no longer point readers to retired template naming"
    - "Planning codebase maps describe active host/module architecture rather than deleted nixos file paths"
    - "Future plan/execution agents can rely on .planning/codebase context without legacy path confusion"
  artifacts:
    - path: "docs/architecture.md"
      provides: "Canonical architecture docs with migrated secret scaffold references"
    - path: "docs/plan.md"
      provides: "Strategic planning doc aligned to current secret template naming"
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Updated machine-readable architecture map for GSD planning"
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "Updated structure map reflecting active directories and files"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Current technical debt map without resolved legacy-file references"
  key_links:
    - from: "docs/architecture.md"
      to: "secrets/common.template.yaml"
      via: "secrets architecture section"
      pattern: "secrets/common"
    - from: ".planning/codebase/ARCHITECTURE.md"
      to: "hosts/oci-melb-1/default.nix"
      via: "composition layer description"
      pattern: "hosts/oci-melb-1/default.nix"
    - from: ".planning/codebase/STRUCTURE.md"
      to: "modules/providers/oci/default.nix"
      via: "directory/file map"
      pattern: "modules/providers/oci/default.nix"
---

<objective>
Reconcile canonical docs and planner-support maps with the post-migration architecture and secret scaffold naming.

Purpose: keep docs and machine-consumed planning context aligned so upcoming phases do not inherit stale `nixos/` assumptions or retired secret template references.
Output: updated `docs/` references plus refreshed `.planning/codebase` architecture maps.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1.1-legacy-config-migration-cleanup/01.1.1-RESEARCH.md
@docs/architecture.md
@docs/plan.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md

<interfaces>
From `docs/architecture.md` (must remain canonical):

```markdown
- `hosts/oci-melb-1/default.nix` as active host entrypoint
- `modules/providers/oci/default.nix` as provider boundary
- `modules/storage/disko-root.nix` as storage boundary
```

From `.planning/codebase/ARCHITECTURE.md` (must be corrected):

```markdown
- currently describes `nixos/configuration.nix`, `nixos/digitalocean.nix`, `nixos/disko-config.nix` as active
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align canonical docs with migrated secret scaffold naming</name>
  <read_first>docs/architecture.md, docs/plan.md, .sops.yaml, secrets/common.template.yaml</read_first>
  <files>docs/architecture.md, docs/plan.md</files>
  <action>Update `docs/architecture.md` and `docs/plan.md` so secret scaffold references explicitly use `secrets/common.yaml` (canonical common secret file) and mention host-scoped `hosts/&lt;host&gt;/secrets.yaml` as the path for host-specific values. Remove or replace any references to `secrets/secrets.template.yaml` so docs only point to `secrets/common.template.yaml` for unencrypted examples. Keep existing migration constraints and documentation-maintenance rule language intact.</action>
  <acceptance_criteria>`docs/architecture.md` contains `secrets/common.yaml` and `hosts/&lt;host&gt;/secrets.yaml`; `docs/plan.md` contains `secrets/common.template.yaml` (or equivalent explicit common-template reference); neither file contains `secrets/secrets.template.yaml`.</acceptance_criteria>
  <verify>
    <automated>rg "secrets/common.yaml|hosts/<host>/secrets.yaml|secrets/common.template.yaml" docs/architecture.md docs/plan.md && ! rg -q "secrets/secrets.template.yaml" docs/architecture.md docs/plan.md</automated>
  </verify>
  <done>Canonical docs describe the migrated secret scaffold and no longer advertise the retired template path.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh planner codebase maps to post-01.1 architecture reality</name>
  <read_first>.planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/CONCERNS.md, flake.nix, hosts/oci-melb-1/default.nix, modules/providers/oci/default.nix, modules/storage/disko-root.nix</read_first>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/CONCERNS.md</files>
  <action>Rewrite the three `.planning/codebase` docs so they reflect the current active architecture: `flake.nix` + `hosts/oci-melb-1/default.nix` + `modules/*` boundaries, and absence of active `nixos/configuration.nix`, `nixos/digitalocean.nix`, `nixos/disko-config.nix`. In concerns, replace resolved legacy-file issues with current cleanup debt relevant after this phase (for example transitional secret filename compatibility and upcoming Phase 2 secret split). Ensure each file clearly references current active paths and removes statements that claim deleted `nixos/*.nix` files are still used.</action>
  <acceptance_criteria>Each refreshed map file contains `hosts/oci-melb-1/default.nix` plus `modules/providers/oci/default.nix` and `modules/storage/disko-root.nix`; none of the three files state that `nixos/configuration.nix`, `nixos/digitalocean.nix`, or `nixos/disko-config.nix` are active evaluated paths.</acceptance_criteria>
  <verify>
    <automated>rg "hosts/oci-melb-1/default.nix|modules/providers/oci/default.nix|modules/storage/disko-root.nix" .planning/codebase/ARCHITECTURE.md .planning/codebase/STRUCTURE.md .planning/codebase/CONCERNS.md && ! rg -q "active.*nixos/configuration.nix|active.*nixos/digitalocean.nix|active.*nixos/disko-config.nix" .planning/codebase/ARCHITECTURE.md .planning/codebase/STRUCTURE.md .planning/codebase/CONCERNS.md</automated>
  </verify>
  <done>Planner support maps now represent current repository reality and are safe to use as planning context.</done>
</task>

</tasks>

<verification>
Run `nix flake check --no-build --no-write-lock-file path:.` and one no-link host build to ensure docs-only cleanup did not regress declarative evaluation.
</verification>

<success_criteria>
- Canonical docs reference common + host-scoped secret scaffold naming.
- `.planning/codebase` maps describe active `hosts/` + `modules/` architecture only.
- No planner-support doc claims deleted `nixos/*.nix` files are active.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1.1-legacy-config-migration-cleanup/01.1.1-legacy-config-migration-cleanup-02-SUMMARY.md`
</output>
