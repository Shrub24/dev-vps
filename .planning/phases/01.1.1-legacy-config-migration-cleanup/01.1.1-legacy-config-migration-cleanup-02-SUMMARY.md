---
phase: 01.1.1-legacy-config-migration-cleanup
plan: 02
subsystem: docs
tags: [architecture, planning, secrets, nixos]
requires:
  - phase: 01.1.1-01
    provides: migrated secret scaffold naming and SOPS path policy
provides:
  - Canonical docs aligned to common and host-scoped secret naming
  - Updated planning codebase maps for hosts/modules architecture
affects: [phase-02-secrets-and-bootstrap, future-gsd-planning]
tech-stack:
  added: []
  patterns: [docs-and-codebase-map-coevolution, host-module-boundary-documentation]
key-files:
  created: []
  modified: [docs/architecture.md, docs/plan.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/CONCERNS.md]
key-decisions:
  - "Keep canonical docs explicitly anchored on secrets/common.yaml + hosts/<host>/secrets.yaml and common template guidance."
  - "Rewrite planning codebase maps to match the active flake->hosts->modules composition and remove stale legacy-path assumptions."
patterns-established:
  - "Planning maps must track active evaluated paths after each architecture migration step"
requirements-completed: [REPO-02, OPER-02]
duration: 13min
completed: 2026-03-21
---

# Phase 01.1.1 Plan 02: Legacy Config Migration Cleanup Summary

**Canonical docs and planning maps now describe the active flake-plus-hosts/modules architecture and migrated secret scaffold naming.**

## Performance

- **Duration:** 13 min
- **Started:** 2026-03-21T04:40:00Z
- **Completed:** 2026-03-21T04:53:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Updated `docs/architecture.md` and `docs/plan.md` to explicitly reference `secrets/common.yaml`, `hosts/<host>/secrets.yaml`, and `secrets/common.template.yaml`.
- Rewrote `.planning/codebase/ARCHITECTURE.md` to reflect active `flake.nix` -> `hosts/oci-melb-1/default.nix` -> `modules/*` composition.
- Rewrote `.planning/codebase/STRUCTURE.md` and `.planning/codebase/CONCERNS.md` to remove stale legacy-path assumptions and capture current post-migration debt.

## Task Commits

Each task was committed atomically:

1. **Task 1: Align canonical docs with migrated secret scaffold naming** - `e00628e` (docs)
2. **Task 2: Refresh planner codebase maps to post-01.1 architecture reality** - `cff6ab3` (docs)

## Files Created/Modified
- `docs/architecture.md` - added explicit common-template reference under secrets architecture.
- `docs/plan.md` - added explicit common/host secret path and template references in strategic track B.
- `.planning/codebase/ARCHITECTURE.md` - refreshed architecture map to current host/module composition.
- `.planning/codebase/STRUCTURE.md` - refreshed structure map to current directories and placement rules.
- `.planning/codebase/CONCERNS.md` - replaced resolved legacy-file debt with current transitional concerns.

## Decisions Made
- Maintain canonical docs and machine-consumed planning maps together in the same change window so future planning agents read current architectural truth.
- Keep transitional secret compatibility debt visible in concerns docs until full Phase 2 secret split is completed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Target host build verification blocked by local architecture/trust restrictions**
- **Found during:** Plan-level verification after Task 2
- **Issue:** `nix build --no-link path:.#nixosConfigurations.oci-melb-1.config.system.build.toplevel` requires `aarch64-linux` build steps, but executor host is `x86_64-linux`, and untrusted user restrictions reject `--system` / `--always-allow-substitutes` overrides.
- **Fix Attempts:**
  1. Retried build with `--always-allow-substitutes`
  2. Retried build with explicit `--system aarch64-linux`
  3. Re-ran build with both constraints acknowledged by Nix warnings
- **Outcome:** All 3 attempts failed with the same architecture/trust gate; issue deferred and logged.
- **Deferred log:** `.planning/phases/01.1.1-legacy-config-migration-cleanup/deferred-items.md`

---

**Total deviations:** 1 auto-fixed/deferred (Rule 3 blocking verification environment)
**Impact on plan:** Documentation and mapping outputs completed as planned; one environment-specific verification command remains deferred.

## Deferred Issues

- No-link `aarch64` host build cannot complete in current local executor permissions/environment; run the same command on an `aarch64-linux` or trusted Nix builder to close verification debt.

## Issues Encountered

- Local executor is not trusted for restricted Nix settings needed to bypass architecture build constraints.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Canonical docs and planning maps now align with active architecture and are safe inputs for Phase 2 planning and execution.
- Remaining verification debt is explicitly tracked in `deferred-items.md`.

## Self-Check: PASSED

---
*Phase: 01.1.1-legacy-config-migration-cleanup*
*Completed: 2026-03-21*
