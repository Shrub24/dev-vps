---
phase: 01.1.1
slug: legacy-config-migration-cleanup
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-21
---

# Phase 01.1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Static CLI verification (`rg`, `test`, `nix flake check`) |
| **Config file** | none — command-level validation only |
| **Quick run command** | `nix flake check --no-build --no-write-lock-file path:.` |
| **Full suite command** | `nix build --no-link --no-write-lock-file path:.#nixosConfigurations.oci-melb-1.config.system.build.toplevel` |
| **Estimated runtime** | ~30-60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `nix flake check --no-build --no-write-lock-file path:.`
- **After every plan wave:** Run `nix build --no-link --no-write-lock-file path:.#nixosConfigurations.oci-melb-1.config.system.build.toplevel`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 01.1.1-01-01 | 01 | 1 | REPO-02 | static-check | `! rg -q "codenomad|tailscale-serve-codenomad" justfile && rg "logs unit=\"tailscaled\"" justfile` | ✅ | ⬜ pending |
| 01.1.1-01-02 | 01 | 1 | REPO-02 | static-check | `test -f secrets/common.template.yaml && ! test -f secrets/secrets.template.yaml && rg "path_regex: \^secrets/(common|secrets)\\.ya\?ml\$" .sops.yaml` | ✅ | ⬜ pending |
| 01.1.1-02-01 | 02 | 2 | OPER-02 | docs-check | `rg "secrets/common\.yaml|hosts/<host>/secrets\.yaml|secrets/common\.template\.yaml" docs/architecture.md docs/plan.md && ! rg -q "secrets/secrets\.template\.yaml" docs/architecture.md docs/plan.md` | ✅ | ⬜ pending |
| 01.1.1-02-02 | 02 | 2 | REPO-02, OPER-02 | map-check | `! rg -q "nixos/configuration.nix|nixos/digitalocean.nix|nixos/disko-config.nix" .planning/codebase/ARCHITECTURE.md .planning/codebase/STRUCTURE.md .planning/codebase/CONCERNS.md` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements.

---

## Manual-Only Verifications

All phase behaviors have automated verification.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
