---
phase: 01.1.1-legacy-config-migration-cleanup
status: passed
verified: 2026-03-21
requirements_checked: [REPO-02, OPER-02]
plans_verified: [01.1.1-01, 01.1.1-02]
---

# Phase 01.1.1 Verification

Phase 01.1.1 goal is met: legacy operator/secret scaffold naming was cleaned up, canonical docs reflect migrated secret paths, and `.planning/codebase` maps now describe the active hosts/modules architecture.

## Must-Have Checks

1. Operator recipes use active service naming: **PASS**
   - Evidence: `justfile` default logs recipe now uses `unit="tailscaled"`.
   - Evidence: no `codenomad` or `tailscale-serve-codenomad` references remain in `justfile`.

2. Secret scaffolding migrated to common naming with host path support: **PASS**
   - Evidence: `secrets/common.template.yaml` exists and `secrets/secrets.template.yaml` is removed.
   - Evidence: `.sops.yaml` includes transition-safe `secrets/(common|secrets).yaml` pattern and host-scoped `hosts/<host>/secrets.yaml` pattern.

3. Canonical docs reference common and host-scoped secret paths: **PASS**
   - Evidence: `docs/architecture.md` and `docs/plan.md` reference `secrets/common.yaml`, `hosts/<host>/secrets.yaml`, and `secrets/common.template.yaml`.
   - Evidence: no retired `secrets/secrets.template.yaml` references remain in these canonical docs.

4. Planning codebase maps reflect current architecture: **PASS**
   - Evidence: `.planning/codebase/ARCHITECTURE.md`, `.planning/codebase/STRUCTURE.md`, `.planning/codebase/CONCERNS.md` reference `hosts/oci-melb-1/default.nix`, `modules/providers/oci/default.nix`, and `modules/storage/disko-root.nix`.
   - Evidence: map files no longer describe deleted `nixos/*.nix` paths as active architecture.

5. Requirements coverage for this phase: **PASS**
   - REPO-02: complete
   - OPER-02: complete

## Verification Commands Run

- `rg "logs unit=\"tailscaled\"" justfile`
- `! rg -q "codenomad|tailscale-serve-codenomad" justfile`
- `test -f secrets/common.template.yaml && test ! -f secrets/secrets.template.yaml`
- `rg "path_regex: \^secrets/(common|secrets)\\.ya\?ml\$|path_regex: \^hosts/[^/]+/secrets\\.ya\?ml\$" .sops.yaml`
- `rg "secrets/common.yaml|hosts/<host>/secrets.yaml|secrets/common.template.yaml" docs/architecture.md docs/plan.md`
- `! rg -q "secrets/secrets.template.yaml" docs/architecture.md docs/plan.md`
- `rg "hosts/oci-melb-1/default.nix|modules/providers/oci/default.nix|modules/storage/disko-root.nix" .planning/codebase/ARCHITECTURE.md .planning/codebase/STRUCTURE.md .planning/codebase/CONCERNS.md`
- `! rg -q "active.*nixos/configuration.nix|active.*nixos/digitalocean.nix|active.*nixos/disko-config.nix" .planning/codebase/ARCHITECTURE.md .planning/codebase/STRUCTURE.md .planning/codebase/CONCERNS.md`
- `nix flake check --no-build --no-write-lock-file path:.`

## Verification Debt

- `nix build --no-link --no-write-lock-file path:.#nixosConfigurations.oci-melb-1.config.system.build.toplevel` could not run in this executor environment due `x86_64-linux` host + untrusted-user restriction for restricted build flags. Deferred in `.planning/phases/01.1.1-legacy-config-migration-cleanup/deferred-items.md`.

## Human Verification

None required for this phase.
