---
phase: 03-oci-host-bring-up-and-private-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy.sh
  - tests/phase-03-bootstrap-contract.sh
  - .planning/phases/03-oci-host-bring-up-and-private-operations/03-BOOTSTRAP.md
autonomous: true
requirements:
  - BOOT-01
  - BOOT-02
  - STOR-01
must_haves:
  truths:
    - "Operator can bootstrap oci-melb-1 remotely with the repository-defined nixos-anywhere flow."
    - "Bootstrap path still uses declarative disko layout instead of manual partitioning."
    - "Host storage contract includes exactly one persistent service mount at /srv/data with stable labels."
  artifacts:
    - path: "deploy.sh"
      provides: "Canonical remote install command contract for oci-melb-1"
    - path: "tests/phase-03-bootstrap-contract.sh"
      provides: "Executable assertions for install and disko storage invariants"
      exports: ["bash contract exit 0"]
    - path: ".planning/phases/03-oci-host-bring-up-and-private-operations/03-BOOTSTRAP.md"
      provides: "Operator bootstrap runbook aligned to deploy.sh contract"
  key_links:
    - from: "deploy.sh"
      to: "flake.nix"
      via: "--flake path:.#oci-melb-1"
      pattern: "--flake \"path:.#oci-melb-1\""
    - from: "hosts/oci-melb-1/default.nix"
      to: "modules/storage/disko-root.nix"
      via: "host imports"
      pattern: "modules/storage/disko-root.nix"
    - from: "modules/storage/disko-root.nix"
      to: "/srv/data"
      via: "disko ext4 partition mountpoint"
      pattern: "mountpoint = \"/srv/data\""
---

<objective>
Lock bootstrap and storage contracts so Phase 3 starts from executable guarantees instead of assumptions.

Purpose: make BOOT-01/BOOT-02/STOR-01 provable with fast automated checks and a single operator runbook.
Output: bootstrap contract test, canonical bootstrap runbook, and explicit invariants for OCI install + disko layout.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-oci-host-bring-up-and-private-operations/03-RESEARCH.md
@deploy.sh
@hosts/oci-melb-1/default.nix
@modules/storage/disko-root.nix

<interfaces>
Current install contract in `deploy.sh` to preserve:

```bash
nix run github:nix-community/nixos-anywhere -- \
  --flake "path:.#oci-melb-1" \
  --build-on-remote \
  --target-host "root@${TARGET_HOST}"
```

Current storage contract in `modules/storage/disko-root.nix`:

```nix
root = { format = "ext4"; extraArgs = [ "-L" "rootfs" ]; mountpoint = "/"; };
data = { format = "ext4"; extraArgs = [ "-L" "srv-data" ]; mountpoint = "/srv/data"; };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing-first bootstrap contract test scaffold</name>
  <read_first>tests/phase-02-03-host-contract.sh, deploy.sh, hosts/oci-melb-1/default.nix, modules/storage/disko-root.nix</read_first>
  <files>tests/phase-03-bootstrap-contract.sh</files>
  <behavior>
    - Test fails when deploy.sh loses `nixos-anywhere`, `--build-on-remote`, or `--flake "path:.#oci-melb-1"` contract.
    - Test fails when host import no longer references `modules/storage/disko-root.nix`.
    - Test fails when disko storage no longer contains `/` label `rootfs` and `/srv/data` label `srv-data`.
  </behavior>
  <action>Create executable shell test `tests/phase-03-bootstrap-contract.sh` with strict mode and `rg --fixed-strings --quiet` assertions for: `nix run github:nix-community/nixos-anywhere --`, `--flake "path:.#oci-melb-1"`, `--build-on-remote`, `--target-host "root@${TARGET_HOST}"` in `deploy.sh`; `../../modules/storage/disko-root.nix` in `hosts/oci-melb-1/default.nix`; and `mountpoint = "/"`, `mountpoint = "/srv/data"`, `extraArgs = [ "-L" "rootfs" ]`, `extraArgs = [ "-L" "srv-data" ]` in `modules/storage/disko-root.nix`.</action>
  <acceptance_criteria>`tests/phase-03-bootstrap-contract.sh` exists, is executable, and contains all eight fixed-string assertions listed in action text.</acceptance_criteria>
  <verify>
    <automated>bash tests/phase-03-bootstrap-contract.sh</automated>
  </verify>
  <done>Bootstrap and disk-layout invariants are enforced by an automated contract test.</done>
</task>

<task type="auto">
  <name>Task 2: Document canonical OCI bootstrap flow linked to contract checks</name>
  <read_first>deploy.sh, justfile, .planning/phases/02-oci-bootstrap-and-service-readiness/02-SERVICE-READINESS.md, .planning/phases/03-oci-host-bring-up-and-private-operations/03-RESEARCH.md</read_first>
  <files>.planning/phases/03-oci-host-bring-up-and-private-operations/03-BOOTSTRAP.md</files>
  <action>Create `.planning/phases/03-oci-host-bring-up-and-private-operations/03-BOOTSTRAP.md` with exact operator steps: (1) run `just flake-check`; (2) run `bash tests/phase-03-bootstrap-contract.sh`; (3) run `./deploy.sh <oci_public_ip_or_dns> [extra-files]`; (4) validate host name using `nix eval --raw path:.#nixosConfigurations.oci-melb-1.config.networking.hostName`; (5) explicitly state this bootstrap remains Tailscale-first and does not introduce public ingress. Include a troubleshooting subsection with exact checks `lsblk -f`, `findmnt /srv/data`, and `systemctl status tailscaled` for first-boot validation.</action>
  <acceptance_criteria>Bootstrap doc exists and contains the exact commands `just flake-check`, `bash tests/phase-03-bootstrap-contract.sh`, `./deploy.sh`, `lsblk -f`, `findmnt /srv/data`, and `systemctl status tailscaled`.</acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/03-oci-host-bring-up-and-private-operations/03-BOOTSTRAP.md && rg "just flake-check|bash tests/phase-03-bootstrap-contract.sh|\./deploy.sh|lsblk -f|findmnt /srv/data|systemctl status tailscaled" .planning/phases/03-oci-host-bring-up-and-private-operations/03-BOOTSTRAP.md</automated>
  </verify>
  <done>Operator has one concrete bootstrap runbook that maps directly to enforced command contracts.</done>
</task>

</tasks>

<verification>
Run `bash tests/phase-03-bootstrap-contract.sh` and ensure it exits 0 before any host install attempt.
</verification>

<success_criteria>
- BOOT-01 is enforced by a scripted check over canonical deploy path.
- BOOT-02 and STOR-01 are enforced by scripted checks over disko import + mount/label invariants.
- Bootstrap runbook references only tested commands and no public-exposure steps.
</success_criteria>

<output>
After completion, create `.planning/phases/03-oci-host-bring-up-and-private-operations/03-oci-host-bring-up-and-private-operations-01-SUMMARY.md`
</output>
