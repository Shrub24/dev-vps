---
phase: 03-oci-host-bring-up-and-private-operations
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - justfile
  - tests/phase-03-operations-contract.sh
  - .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md
autonomous: true
requirements:
  - BOOT-03
  - OPER-01
  - STOR-02
must_haves:
  truths:
    - "Operator can run a repeatable host-targeted update path for oci-melb-1 after install."
    - "Operator has one command sequence for preflight, update, and post-update verification."
    - "Service data paths remain predictable under /srv/data for ongoing operations."
  artifacts:
    - path: "justfile"
      provides: "Canonical day-2 update and phase-03 verification commands"
    - path: "tests/phase-03-operations-contract.sh"
      provides: "Executable checks for redeploy command and service path invariants"
    - path: ".planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md"
      provides: "Operator runbook for routine host-targeted updates"
  key_links:
    - from: "justfile"
      to: "nixpkgs#nixos-rebuild"
      via: "redeploy recipe"
      pattern: "nix run nixpkgs#nixos-rebuild -- switch"
    - from: "justfile"
      to: "tests/phase-03-*.sh"
      via: "phase verification recipe"
      pattern: "bash tests/phase-03-"
    - from: "modules/services/*.nix"
      to: "/srv/data"
      via: "service directory settings"
      pattern: "/srv/data/(syncthing|media|navidrome|inbox)"
---

<objective>
Finalize day-2 operations so rebuilds and storage/service path guarantees are repeatable from one operator workflow.

Purpose: satisfy BOOT-03, OPER-01, and STOR-02 by making update commands and path invariants executable.
Output: consolidated ops commands, operations contract test, and routine update runbook.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-oci-host-bring-up-and-private-operations/03-RESEARCH.md
@justfile
@modules/services/syncthing.nix
@modules/services/navidrome.nix
@modules/services/slskd.nix
@tests/phase-03-bootstrap-contract.sh
@tests/phase-03-access-contract.sh

<interfaces>
Current day-2 update contract in `justfile`:

```just
redeploy:
  nix run nixpkgs#nixos-rebuild -- switch --flake path:.#oci-melb-1 --sudo --target-host {{target_user}}@{{target_host}} --build-host {{target_user}}@{{target_host}}
```

Service-path contract to preserve:

```text
/srv/data/syncthing/config
/srv/data/media
/srv/data/navidrome
/srv/data/inbox
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add operations contract test for update path and storage directories</name>
  <read_first>justfile, modules/services/syncthing.nix, modules/services/navidrome.nix, modules/services/slskd.nix, tests/phase-03-bootstrap-contract.sh, tests/phase-03-access-contract.sh</read_first>
  <files>tests/phase-03-operations-contract.sh</files>
  <behavior>
    - Test fails if `redeploy` no longer targets `path:.#oci-melb-1` through `nixos-rebuild -- switch`.
    - Test fails if update command stops using `--target-host` and `--build-host` host-targeted workflow.
    - Test fails if service path contracts move away from `/srv/data` directories.
  </behavior>
  <action>Create executable `tests/phase-03-operations-contract.sh` asserting in `justfile`: `redeploy:`, `nix run nixpkgs#nixos-rebuild -- switch`, `--flake path:.#oci-melb-1`, `--target-host {{target_user}}@{{target_host}}`, `--build-host {{target_user}}@{{target_host}}`; and asserting in service modules: `/srv/data/syncthing/config` and `/srv/data/media` in `modules/services/syncthing.nix`, `/srv/data/navidrome` in `modules/services/navidrome.nix`, and `/srv/data/inbox/complete` plus `/srv/data/inbox/incomplete` in `modules/services/slskd.nix`.</action>
  <acceptance_criteria>`tests/phase-03-operations-contract.sh` exists, executable, and checks all justfile + service path invariants listed in action text.</acceptance_criteria>
  <verify>
    <automated>bash tests/phase-03-operations-contract.sh</automated>
  </verify>
  <done>Update workflow and `/srv/data` service directory contracts are machine-checked.</done>
</task>

<task type="auto">
  <name>Task 2: Add consolidated phase-03 verification and routine update runbook</name>
  <read_first>justfile, .planning/phases/03-oci-host-bring-up-and-private-operations/03-BOOTSTRAP.md, .planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md, tests/phase-03-bootstrap-contract.sh, tests/phase-03-access-contract.sh, tests/phase-03-operations-contract.sh</read_first>
  <files>justfile, .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md</files>
  <action>Update `justfile` by adding recipe `verify-phase-03:` that runs exactly `bash tests/phase-03-bootstrap-contract.sh`, `bash tests/phase-03-access-contract.sh`, `bash tests/phase-03-operations-contract.sh`, then `just verify-oci-contract`. Create `.planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md` with routine day-2 sequence: `just verify-phase-03` -> `just redeploy` -> `just status` -> `just tailscale-status`. Include rollback instruction referencing break-glass doc path and explicitly state this phase keeps deployment workflow host-targeted (no deploy-rs adoption yet).</action>
  <acceptance_criteria>`justfile` contains `verify-phase-03:` and the four exact command lines; operations doc exists and contains `just verify-phase-03`, `just redeploy`, `just status`, `just tailscale-status`, and text `no deploy-rs adoption yet`.</acceptance_criteria>
  <verify>
    <automated>rg "verify-phase-03:|bash tests/phase-03-bootstrap-contract.sh|bash tests/phase-03-access-contract.sh|bash tests/phase-03-operations-contract.sh|just verify-oci-contract" justfile && test -f .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md && rg "just verify-phase-03|just redeploy|just status|just tailscale-status|no deploy-rs adoption yet" .planning/phases/03-oci-host-bring-up-and-private-operations/03-OPERATIONS.md</automated>
  </verify>
  <done>Operator has one repeatable, host-targeted update workflow with pre/post checks and explicit rollback pointer.</done>
</task>

</tasks>

<verification>
Run `just verify-phase-03` then `just redeploy` against target host during execution to validate BOOT-03/OPER-01 operational loop.
</verification>

<success_criteria>
- BOOT-03 and OPER-01 are executable through one host-targeted command path in justfile.
- STOR-02 is guarded by contract checks over service directories rooted under `/srv/data`.
- Phase-level verification can be run with a single `just verify-phase-03` command.
</success_criteria>

<output>
After completion, create `.planning/phases/03-oci-host-bring-up-and-private-operations/03-oci-host-bring-up-and-private-operations-03-SUMMARY.md`
</output>
