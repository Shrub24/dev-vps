---
phase: 03-oci-host-bring-up-and-private-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/services/tailscale.nix
  - tests/phase-03-access-contract.sh
  - .planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md
autonomous: true
requirements:
  - ACCS-01
  - ACCS-02
  - SRVC-01
must_haves:
  truths:
    - "Operator can keep access private over Tailscale with no public service exposure introduced in this phase."
    - "Tailscale remains declaratively enabled from NixOS module state."
    - "Operator has a repository-tracked break-glass path when Tailscale/SSH access fails."
  artifacts:
    - path: "modules/services/tailscale.nix"
      provides: "Explicit private-first declarative tailscale service contract"
    - path: "tests/phase-03-access-contract.sh"
      provides: "Executable assertions for access and privacy invariants"
    - path: ".planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md"
      provides: "Break-glass recovery runbook with serial-console recovery steps"
  key_links:
    - from: "modules/services/tailscale.nix"
      to: "hosts/oci-melb-1/default.nix"
      via: "host imports"
      pattern: "modules/services/tailscale.nix"
    - from: "hosts/oci-melb-1/default.nix"
      to: "networking.firewall.trustedInterfaces"
      via: "tailscale-only trust boundary"
      pattern: "trustedInterfaces = [ \"tailscale0\" ]"
    - from: ".planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md"
      to: "OCI console"
      via: "serial-console recovery flow"
      pattern: "serial console"
---

<objective>
Codify private access posture and recovery path so Tailscale-first operations stay safe and recoverable.

Purpose: satisfy ACCS-01, ACCS-02, and SRVC-01 with explicit module defaults, automated contract checks, and a break-glass runbook.
Output: hardened tailscale module defaults, access contract test, and documented serial-console recovery flow.
</objective>

<execution_context>
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/LinuxData/Projects/dev/dev-vps/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-oci-host-bring-up-and-private-operations/03-RESEARCH.md
@modules/services/tailscale.nix
@hosts/oci-melb-1/default.nix
@modules/services/navidrome.nix
@modules/services/slskd.nix

<interfaces>
Current tailscale module contract:

```nix
{ ... }:
{
  services.tailscale.enable = true;
}
```

Current host trust-boundary contract:

```nix
networking.firewall.trustedInterfaces = [ "tailscale0" ];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit private-first tailscale defaults and access contract test</name>
  <read_first>modules/services/tailscale.nix, hosts/oci-melb-1/default.nix, modules/services/navidrome.nix, modules/services/slskd.nix, tests/phase-02-03-host-contract.sh</read_first>
  <files>modules/services/tailscale.nix, tests/phase-03-access-contract.sh</files>
  <behavior>
    - Access contract fails if tailscale service is no longer explicitly enabled.
    - Access contract fails if host no longer trusts only `tailscale0`.
    - Access contract fails if service modules open firewall ports publicly.
  </behavior>
  <action>Update `modules/services/tailscale.nix` to declare `services.tailscale = { enable = true; openFirewall = false; };` so private posture is explicit instead of implicit. Create executable `tests/phase-03-access-contract.sh` with fixed-string assertions for: `services.tailscale = {` + `enable = true;` + `openFirewall = false;` in `modules/services/tailscale.nix`; `trustedInterfaces = [ "tailscale0" ]` in `hosts/oci-melb-1/default.nix`; and `openFirewall = false;` in both `modules/services/navidrome.nix` and `modules/services/slskd.nix`.</action>
  <acceptance_criteria>`modules/services/tailscale.nix` contains `openFirewall = false;` and `tests/phase-03-access-contract.sh` exists, executable, and checks all five invariants listed in action text.</acceptance_criteria>
  <verify>
    <automated>bash tests/phase-03-access-contract.sh</automated>
  </verify>
  <done>Private-only access posture is encoded in module defaults and enforced by automated contract checks.</done>
</task>

<task type="auto">
  <name>Task 2: Create break-glass recovery runbook with command-level steps</name>
  <read_first>.planning/phases/03-oci-host-bring-up-and-private-operations/03-RESEARCH.md, hosts/oci-melb-1/default.nix, justfile</read_first>
  <files>.planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md</files>
  <action>Create `.planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md` containing: trigger conditions (Tailscale down, SSH unreachable), OCI serial console access steps, single-user recovery flow, and rollback commands. Include exact rollback/recovery commands: `nix-env -p /nix/var/nix/profiles/system --list-generations`, `sudo /nix/var/nix/profiles/system-<generation>-link/bin/switch-to-configuration switch`, `sudo systemctl restart tailscaled`, and `sudo tailscale status`. Add a post-recovery validation checklist requiring `hostnamectl`, `systemctl is-active tailscaled`, and successful `just tailscale-status` output from operator machine.</action>
  <acceptance_criteria>Break-glass doc exists and contains the literal strings `serial console`, `switch-to-configuration switch`, `systemctl restart tailscaled`, `tailscale status`, and `just tailscale-status`.</acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md && rg "serial console|switch-to-configuration switch|systemctl restart tailscaled|tailscale status|just tailscale-status" .planning/phases/03-oci-host-bring-up-and-private-operations/03-BREAKGLASS.md</automated>
  </verify>
  <done>ACCS-02 has a concrete, repository-tracked break-glass procedure with executable commands.</done>
</task>

</tasks>

<verification>
Run `bash tests/phase-03-access-contract.sh` and confirm break-glass runbook command set is present via grep.
</verification>

<success_criteria>
- ACCS-01 and SRVC-01 are enforced with explicit private-first service defaults and tests.
- ACCS-02 is covered by a command-level break-glass path in phase artifacts.
- No step adds public ingress as part of this phase plan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-oci-host-bring-up-and-private-operations/03-oci-host-bring-up-and-private-operations-02-SUMMARY.md`
</output>
