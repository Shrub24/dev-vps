# Unencrypted template for reference only.
# Encrypt real values into hosts/do-admin-1/secrets.yaml with sops.
tailscale:
  auth_key: REPLACE_WITH_TAILSCALE_AUTH_KEY
cloudflare:
  dns_api_token: REPLACE_WITH_CLOUDFLARE_DNS_API_TOKEN
cloudflare_access:
  upstream_oidc:
    client_id: REPLACE_WITH_CLOUDFLARE_ACCESS_UPSTREAM_OIDC_CLIENT_ID
    client_secret: REPLACE_WITH_CLOUDFLARE_ACCESS_UPSTREAM_OIDC_CLIENT_SECRET
pocket_id:
  encryption_key: REPLACE_WITH_POCKET_ID_ENCRYPTION_KEY
  apps:
    gatus:
      client_id: REPLACE_WITH_GATUS_OIDC_CLIENT_ID
      client_secret: REPLACE_WITH_GATUS_OIDC_CLIENT_SECRET
    termix:
      client_id: REPLACE_WITH_TERMIX_OIDC_CLIENT_ID
      client_secret: REPLACE_WITH_TERMIX_OIDC_CLIENT_SECRET

homepage:
  tailscale:
    device_id: REPLACE_WITH_HOMEPAGE_TAILSCALE_DEVICE_ID
    api_key: REPLACE_WITH_HOMEPAGE_TAILSCALE_API_KEY
  navidrome:
    user: REPLACE_WITH_HOMEPAGE_NAVIDROME_USER
    token: REPLACE_WITH_HOMEPAGE_NAVIDROME_TOKEN
    salt: REPLACE_WITH_HOMEPAGE_NAVIDROME_SALT
  slskd:
    key: REPLACE_WITH_HOMEPAGE_SLSKD_KEY
  beszel:
    username: REPLACE_WITH_HOMEPAGE_BESZEL_USERNAME
    password: REPLACE_WITH_HOMEPAGE_BESZEL_PASSWORD

termix:
  ssh-key: ""
