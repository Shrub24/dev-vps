# Unencrypted template for reference only.
# Encrypt real values into hosts/oci-melb-1/secrets.yaml with sops.
tailscale:
  auth_key: REPLACE_WITH_TAILSCALE_AUTH_KEY
beszel:
  agent:
    token: REPLACE_WITH_BESZEL_AGENT_TOKEN
beets:
  discogs_token: REPLACE_WITH_DISCOGS_USER_TOKEN
soulsync:
  slskd_api_key: REPLACE_WITH_SLSKD_API_KEY
  discogs_token: REPLACE_WITH_DISCOGS_USER_TOKEN
  navidrome_username: REPLACE_WITH_NAVIDROME_USERNAME
  navidrome_password: REPLACE_WITH_NAVIDROME_PASSWORD
slskd:
  slsk_username: REPLACE_WITH_SOULSEEK_USERNAME
  slsk_password: REPLACE_WITH_SOULSEEK_PASSWORD
  web_password: REPLACE_WITH_SLSKD_WEB_PASSWORD
cockpit:
  service_user:
    password_hash: REPLACE_WITH_COCKPIT_SERVICE_USER_PASSWORD_HASH
tagr:
  auth_secret: REPLACE_WITH_TAGR_AUTH_SECRET
  auth_user: REPLACE_WITH_TAGR_AUTH_USER
  auth_password: REPLACE_WITH_TAGR_AUTH_PASSWORD
